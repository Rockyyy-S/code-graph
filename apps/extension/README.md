# CodeGraph extension shell

This package is the side-effect-free VS Code extension shell established by Story 1.1.
It exposes no commands, views, service connection, or product behavior yet. The generated
template is used only as the supported TypeScript + esbuild foundation.
