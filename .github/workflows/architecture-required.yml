name: child-gate-evidence

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  actions: read
  attestations: write
  contents: read
  id-token: write

jobs:
  gate-evidence:
    name: gate-evidence
    uses: Rockyyy-S/code-graph-gate-controller/.github/workflows/produce-gate-evidence.yml@78e84adecc7ef1b73a881dbd4bb6224ce7a7a769
    with:
      base_oid: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
      head_oid: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      object_format: sha1
      provider_repository_id: ${{ format('{0}', github.event.repository.id) }}
      producer_workflow_sha: 78e84adecc7ef1b73a881dbd4bb6224ce7a7a769
      repository: ${{ github.repository }}
