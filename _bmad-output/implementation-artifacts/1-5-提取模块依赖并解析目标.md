---
baseline_commit: 86bd1338d333cedbb0aab19508c786915175eab7
---

# Story 1.5: 提取模块依赖并解析目标

Status: review

<!-- Note: 本 Story 已按 create-story checklist 完成上下文校验，实施后仍需独立 code-review。 -->

## Story

As a 开发者，
I want 图谱提取可解释的 TypeScript/JavaScript 模块依赖并解析目标，
so that 我可以查询可靠的内部、外部和 Node 内置模块关系。

## Acceptance Criteria

1. **Given** 工作区包含 TS、TSX、JS 或 JSX 文件
   **When** Analyzer 处理源码
   **Then** 在 Worker 中使用 TypeScript 6.0.3 稳定公开 Compiler API
   **And** 不加载项目 plugin、transformer、scripts 或 VS Code TypeScript Server 私有状态
   **And** 不使用 TypeScript 7 unstable API、Tree-sitter、LSP 或 SCIP。

2. **Given** Analyzer 构建配置快照
   **When** 解析 tsconfig、jsconfig、extends 链、manifest 和模块解析元数据
   **Then** `consultedFiles` 和 `workspacePackages` 按规范路径排序
   **And** `configDigest` 与 `inputDigest` 使用 RFC 8785 JCS、UTF-8 和 SHA-256
   **And** `rules.yaml` 不进入分析配置。

3. **Given** 源码包含静态 import/export、re-export、type-only、literal `require()` 或 literal `import()`
   **When** Analyzer 规范化语法
   **Then** 按 AD-24 唯一映射为 `imports` 或 `exports` 关系和固定 qualifier
   **And** 非 literal 动态导入不生成伪精确边
   **And** MVP 不生成 `references` 或调用图。

4. **Given** 模块目标可能是 Node built-in、内部文件、外部 npm 包或未解析 bare specifier
   **When** 解析目标
   **Then** 按架构固定优先级选择目标
   **And** Node built-in 使用 `node:` 标识，外部包使用 purl
   **And** 未解析 bare specifier 降低置信度，未解析相对路径只产生诊断。

5. **Given** Analyzer 输出模块依赖事实
   **When** indexing application 规范化结果
   **Then** 每条关系保留方向、qualifier、来源、置信度、文件范围和语言
   **And** 使用 `source:<analyzerKind>:<fileId>` ownership slice
   **And** Analyzer 不直接访问 `GraphStore`。

6. **Given** 相同源码、配置和分析器版本
   **When** 重复 rebuild
   **Then** 模块节点、edge、目标 ID 和 qualifier 保持相同
   **And** 不生成重复边或把静态依赖表述为运行时调用。

## Tasks / Subtasks

- [x] Task 1：先建立失败测试、事实合同与范围护栏（AC: 1–6）
  - [x] 先为 Worker 边界、配置快照、语法映射、目标优先级、source FactBatch、确定性重建和原子提交分别增加 RED 测试；禁止先改生产实现再补覆盖。
  - [x] 测试固定唯一直接依赖为 Story 1.19；不得按 Story 编号大小或文档位置推断依赖。
  - [x] 明确非范围：`BasicSymbolV1`、完整 Evidence 去重/冲突/删除/tombstone、workspace package 节点与聚合、准确率报告、完整 `.codegraphignore` 生命周期、保存后增量、查询/CLI/UI、`references` 和调用图。
  - [x] 对历史 readiness 文档中“Story 1.5 = ignore 配置”的旧快照增加规划防漂移断言或文档说明；实现权威只读当前 `epics.md`、Architecture Spine 与 `StoryDependencyDagV1`。

- [x] Task 2：把核心事实模型最小泛化为 hierarchy 与 module source 共用合同（AC: 3–6）
  - [x] 在 `packages/domain` 定义封闭的模块目标、模块节点、`imports|exports` 关系、版本化 qualifier、语言、置信度、来源范围与分析诊断模型；公共源码范围固定为 0-based UTF-16 code-unit、`[start,end)` 半开区间。
  - [x] 扩展既有图身份构造器以支持 `contains|imports|exports`，关系 ID 必须继续绑定 workspace、方向、端点、关系类型与 qualifier；不得改变已提交 hierarchy 节点/contains 边的既有 ID。
  - [x] 内部目标复用现有 file 节点；外部 npm 包使用标准 npm purl；Node built-in 使用规范 `node:<module>`；禁止创建第三种持久化 `module` 节点或为外部目标伪造 `relativePath`。
  - [x] 将 hierarchy 专属 `FactBatch/GraphPatch` 最小泛化为可表达 source module facts 的核心模型，同时保留现有 hierarchy API 的兼容导出或明确迁移；禁止长期保留 hierarchy/source 两条独立写入模型。
  - [x] 明确定义 `CompositeGraphPatchV1`（或职责等价封闭合同）：共享唯一 `baseGraphRevision`、完整 read-set 与 `patchDigest`，内部包含按 `ownershipSliceId` 规范排序且唯一的多个 slice mutation；slice 枚举顺序不得影响 patch 内容或 digest。
  - [x] `GraphStorePort` 必须一次读取 hierarchy 与全部受影响 source slices 的 committed facts，并通过一次 `commitAtomicGraphUpdate()` 提交整个 composite patch；禁止逐 source slice 读取后分别提交或推进 revision。
  - [x] `detectedAt` 只作为 UTC 观察元数据，不得进入实体/edge ID、`configDigest`、`inputDigest`、`patchDigest` 或“是否产生语义 revision”的判断；相同事实重新观察不得仅因时间变化推进 `graphRevision`。
  - [x] 固定 TypeScript Evidence ID/去重基础为 AD-21 键：`edgeId + provenance + analyzerVersion + sourceFileId + normalizedRange + evidenceKind`；同 batch/replay 只能生成一条语义 Evidence。application diff 忽略新观察的 `detectedAt`，事实未变化时保留已提交时间且不得静默改写同 revision 数据。

- [x] Task 3：定义 `AnalyzerPort`、Analyzer 输入与配置快照边界（AC: 1, 2, 5）
  - [x] 在 `packages/application` 定义基础设施无关的 `AnalyzerPort`、`AnalysisInputV1`、`AnalyzerConfigSnapshotV1` 和 module FactBatch 输入/输出；application/domain 不得导入 TypeScript、`node:fs`、Worker 或 SQLite 类型。
  - [x] `AnalyzerConfigSnapshotV1` 固定包含 `version`、`analyzerKind`、`analyzerVersion`、`effectiveCompilerOptions`、排序后的 `consultedFiles`、`effectiveIgnore:{version,effectiveDigest}` 和排序后的 `workspacePackages`。
  - [x] `consultedFiles` 必须记录 tsconfig/jsconfig extends 链、package/workspace manifest、lockfile 和模块解析实际读取的非源码 `package.json`；语义集合按规范 path/root 排序，compiler option 中语义有序数组保序，禁止 `localeCompare` 参与身份或 digest。
  - [x] 定义 effective compiler options 的规范化白名单：path-like 选项转为工作区相对 POSIX，枚举使用稳定公共值，排除 `configFilePath`、缓存对象、函数与宿主绝对路径；语义有序数组保序，语义集合排序，禁止直接 digest TypeScript 返回的原始对象。
  - [x] graph-service 是配置快照和 canonical digest 的唯一生产者；复用 `packages/contracts` 的 RFC 8785 JCS → UTF-8 → SHA-256 helper 注入 application，Analyzer 不解析 `.codegraphignore`，`rules.yaml` 永不进入分析配置。
  - [x] 配置收集使用两阶段封口或职责等价协议：Worker 通过受控读取边界返回规范化 compiler-options observation 与实际 consulted logical paths；graph-service 对这些路径执行安全读取/hash，冻结 `AnalyzerConfigSnapshotV1` 并计算 `configDigest`；分析 FactBatch 绑定该 digest，提交前重新复核全部 consulted hashes。分析期间新发现且未进入封口快照的配置文件必须使本轮结果失效并重跑。
  - [x] Story 1.5 只建立 `workspacePackages` 的封闭字段；生产快照在 Story 1.9 前固定为 `[]`，不得提前枚举 workspace、创建 package 节点或产生跨包聚合。Story 1.9 再启用 recognized/single/degraded discovery 并填充排序后的 package roots。

- [x] Task 4：实现受限 Worker 与 TypeScript 6.0.3 分析适配器（AC: 1, 2）
  - [x] 在 `packages/adapters/analyzer-typescript` 精确声明运行时依赖 `typescript: 6.0.3`，同步 adapter 外部依赖 allowlist 与正/负依赖边界测试；不得升级根工具链或引入第二解析器。
  - [x] 使用真实 `node:worker_threads` Worker 运行分析，提供有界启动、取消、错误收敛和幂等关闭；Worker 异常不得卡住 graph-service shutdown 或掩盖原始 Job 错误。
  - [x] 仅使用 TypeScript 6.0.3 稳定公开 Compiler API（如 `createIncrementalProgram`/`createLanguageService`、`resolveModuleName` 与公开 AST guards）；不得读取 tsserver 私有状态、项目 plugin、自定义 transformer 或执行 package scripts。
  - [x] 自定义 Compiler/ModuleResolution host 只把 manifest 内受支持源码作为索引事实来源；外部声明文件和 TypeScript lib 可用于解析但不得成为工作区图节点、源码 Evidence 或导出内容。
  - [x] 选定 host-owned 受控文件边界：graph-service/scanner 向 Worker 传递与 manifest hash 同次可信读取绑定的不可变源码/config 字节快照；动态发现的非源码解析元数据只能通过 graph-service 文件读取 broker 获取并记录 consulted path/hash。Worker 禁止直接用任意 `node:fs` 路径访问工作区，只接收逻辑相对路径、opaque ID 和受限字节；绝对物理路径只留在 host 内部。
  - [x] TypeScript 自带 lib 可以从锁定 adapter 安装读取；外部声明文件仅可作为 Analyzer 私有解析输入，不进入 source manifest、Evidence、节点或导出内容。Worker ESM 入口必须使用构建后的 `.js`，构建/发布产物包含 Worker 文件，并以构建产物而非仅 Vitest/tsx 环境做真实启动测试。

- [x] Task 5：实现 AD-24 唯一语法映射（AC: 3, 5, 6）
  - [x] 静态 import、side-effect/default/namespace import、literal `require()`、external import-equals 映射为 `imports(value|type)`；仅字符串 literal `import()` 映射为 `imports(dynamic)`。
  - [x] type/value 只按源码语法判定：statement-level `import type`/`export type`、specifier-level `type` modifier、`ImportEqualsDeclaration.isTypeOnly` 为 type；其余静态语法为 value，不读取 emit 结果或 `SymbolFlags`，混合声明逐 specifier 拆分。
  - [x] `ImportEqualsDeclaration` 仅当 `moduleReference` 为 `ExternalModuleReference` 且 expression 为字符串 literal 时生成模块边；EntityName 内部别名不得生成模块边。
  - [x] 带 `from` 的 named re-export 每个 specifier 生成对应 `imports`，并生成一条 file→target module entity 的 `exports`，qualifier 为 `reexport:<exportedName>:<importedName>:type|value`；`export *`/`export type *` 每个 declaration 生成一条 imports 和一条 `star:type|value` exports。
  - [x] 本地 named/default export 的规范终点是 local symbol；Story 1.5 定义封闭的 `LocalExportBindingSeedV1={sourceFileId,exportedName,localName|default,typeOrValue,language,normalizedRange,stableSortKey}`，仅作为 Analyzer/application 内部交接事实，不写 graph edge。Story 1.6 建立 BasicSymbol 后再提升为最终 exports edge；禁止 placeholder symbol、file→file 假 exports 或提前宣称 `BasicSymbolV1` 完成。
  - [x] qualifier 优先使用封闭结构化 `ModuleQualifierV1` 并定义唯一序列化；若持久化为字符串，exported/imported 名称段必须使用明确的 percent-encoding，避免冒号等字符导致碰撞或不可逆解析。
  - [x] 非 literal dynamic import、非 literal require、运行时调用和 `references` 不生成伪精确规范边；需要提示时只生成稳定、相对路径化的分析诊断。

- [x] Task 6：实现唯一目标解析与稳定外部身份（AC: 4, 6）
  - [x] 固定优先级：Node built-in → TypeScript `resolvedFileName` 位于 indexing root 且属于有效索引 manifest 的 file → 其他 resolved package 的最近 `package.json` 版本 purl → 未解析 bare specifier 的 `pkg:npm/...@unresolved` → 未解析 relative/absolute 仅诊断。`node_modules` 即使物理上位于 root 内也不得因路径前缀被误判为内部 file。
  - [x] bare 与 `node:` 前缀的 Node built-in 统一规范为 `node:<module>`；外部 scoped package 按标准 npm purl percent-encoding 规范化，包管理器不得进入身份。
  - [x] 成功解析到具体内部/外部目标的静态 import/export 为 high；不完整项目上下文或 unresolved bare 为 medium；不得把启发式/动态结果提升为 high。
  - [x] TypeScript path alias、JS/JSX、ESM/CJS、project references 和 package `exports`/模块解析元数据均走同一 TypeScript 权威解析路径；不得另写第二套近似 resolver。
  - [x] 未解析相对/绝对 specifier 不建 edge、不创建假文件节点；诊断必须携带稳定 code、相对路径、范围和建议动作，不泄漏绝对路径或源码正文。
  - [x] root 外 resolved file 缺少合法 npm name/version、`package.json` 无效、非 npm 外部文件、package subpath 或 scoped package 解析失败时，必须生成稳定诊断并拒绝伪造 high-confidence purl；不得退回绝对文件路径身份。

- [x] Task 7：把 source module FactBatch 接入既有 GraphPatch/CAS/原子提交主干（AC: 2, 5, 6）
  - [x] 每个文件输出 `source:typescript:<fileId>` ownership slice，FactBatch 携带 `inputDigest`、`configDigest`、analyzer kind/version、coverage、规范排序的节点/edge/Evidence 描述与诊断；当前 source slice 只拥有该文件产生的 Evidence（Story 1.6 后再拥有 file-scoped symbol），共享规范 edge 不得被伪装为单一 source 独占；Analyzer 不得依赖或调用 `GraphStorePort`。
  - [x] 扩展 `apps/graph-service/src/index-read-set.ts` 与 Job 编排，使 read-set 同时绑定源码 manifest/hash、AnalyzerConfigSnapshot、完整 EffectiveIgnoreSnapshotV1、bootstrap generation、statusEpoch 和 base revision；语义 digest 与并发栅栏继续严格分离。
  - [x] Analyzer 结果必须通过 application 规范化和唯一 `GraphStorePort.commitAtomicGraphUpdate()` 进入 Story 1.19 的 snapshot mutation channel；一次 logical rebuild 的 hierarchy 与全部 source slices 组成一个 patch、一个同步事务和至多一个新 graphRevision，禁止按文件/Analyzer 分别推进 revision，也禁止 Analyzer→SQLite、adapter→Analyzer 或第二队列/第二 writer 旁路。
  - [x] 在提交前分别复核源码 hash、consulted file/hash、完整 ignore snapshot、bootstrap generation 和 base revision；任一变化丢弃旧 batch/patch并沿用 `MAX_STALE_REQUEUE_ATTEMPTS=3` 有界重排，旧 committed revision 保持可读。
  - [x] 扩展 workspace semantic-event 集合：tsconfig/jsconfig、extends 文件、package.json、lockfile 和已记录 consulted files 的变化都必须推进 stale/generation 证明；不能继续只把受支持源码变更视为语义事件。
  - [x] complete source slice 必须支持本次完整 rebuild 中的最小 replacement：源码 import 移除、文件删除或移动后，旧 source Evidence 与由其唯一支撑的旧模块边必须消失；partial 不按缺失删除，failed 不生成 patch。跨来源 Evidence 去重/冲突、显式 tombstone、共享外部节点最后引用回收和通用生命周期仍由 Story 1.7 完成。

- [x] Task 8：新增 SQLite v3 迁移并保持单事务恢复不变量（AC: 4–6）
  - [x] 保留 `001-bootstrap.ts`、`002-deterministic-commit.ts` 不变，新增 v3 migration；继续只使用现有八表，不提前创建 Findings、impact、export 或发布表。
  - [x] `003-module-dependencies.ts` 成为新的顶层只读 preflight/migration orchestrator：接受 absent/v1/v2/v3，按顺序补齐旧 migration，安全拒绝未来版本；v3 reopen 不得先经过只接受 v1/v2 的旧顶层 assert 而被误拒绝。
  - [x] 通过单事务“新表 → 校验复制 → 替换”重建 `nodes`、`edges`、`facts_ownership` 的 CHECK/UNIQUE，禁止尝试原地修改 SQLite CHECK。回填后逐项验证既有 hierarchy ID、contains edge、ownership、Job/read-set/meta 未变化。
  - [x] 演进 `nodes` 以封闭区分 workspace/directory/file/external-package/node-builtin，并保证外部实体没有伪造本地路径；演进 `edges` 以允许 contains/imports/exports，唯一性必须包含 qualifier，允许同端点的 type/value/dynamic 或不同 re-export qualifier 共存。
  - [x] 在现有 `evidence` 表中持久化版本化、可校验的模块关系证据，至少保留稳定 `id`、可联结并受约束的 `edge_id`、provenance、analyzerVersion、sourceFileId、normalizedRange、evidenceKind、confidence、language 和 detectedAt；将 `facts_ownership.fact_kind` 重建为至少允许 `node|edge|evidence`，Evidence ownership 必须通过 `evidence.id` 恢复/校验，不能只埋在 `payload_json`。完整冲突、tombstone 与共享外部节点引用计数生命周期仍由 Story 1.7 演进。
  - [x] 将 hierarchy/source 节点、边、事实 payload、摘要、read-set、patch digest、Job result 和 graphRevision 在同一同步 `better-sqlite3.transaction()` 中提交；任一步失败整体回滚，第二读者只看到旧或新完整 revision。
  - [x] migration/restart 恢复必须交叉校验 workspace 摘要、真实 hierarchy/source 事实、ownership、terminal Job 与 read-set；全图 count 与 hierarchy 子图 count 分开验证，hierarchy 继续满足树不变量，source edge/Evidence/外部节点按各自拓扑校验，`indexedFileCount` 只统计内部 file。不得仅按计数、时间戳或最后一条 Job 猜测已提交状态。
  - [x] 相同 batch、空语义 patch 或 replay 不插入重复节点/边/Evidence、不产生孤立 ownership、不推进 graphRevision；不得改变 WAL、foreign keys、`synchronous=NORMAL`、busy timeout、故障副本与未知未来 schema fail-closed 行为。

- [x] Task 9：同步真实 blocking gate 与完整回归证据（AC: 1–6）
  - [x] 新增独立 Analyzer blocking gate（建议稳定 `gateId/checkId=typescript-module-analysis-v1`），真实运行 Worker、配置 digest、AD-24 映射、目标优先级、source FactBatch、SQLite v3/CAS/原子提交测试；禁止空测试、永久 skip、无断言或固定成功脚本。
  - [x] 先运行公共能力 base/head diff；只有实际变化的 RPC/Schema 才按“一能力一新增 gate/test/fixture/verifier/evidence”更新 `ci/public-capability-gates.v1.json`，不得凭文件变动猜测公共能力。
  - [x] Unit/Golden：TS/TSX/JS/JSX，ESM/CJS，side-effect/default/namespace，type-only/mixed，named/star re-export，literal require/import()，ImportEquals，范围、语言、置信度和 qualifier。
  - [x] Contract/Boundary：`CompositeGraphPatchV1` 多 slice 排序/digest、Evidence ID、`LocalExportBindingSeedV1` 交接、两阶段 config snapshot 封口、compiler options 规范化与 `workspacePackages=[]`。
  - [x] Resolution/Negative：`baseUrl`/`paths`、project references、扩展名与 directory/index 候选、Node built-in、内部文件、外部/Scoped purl、unresolved bare、unresolved relative、non-literal dynamic、语法错误可恢复事实、插件/transformer/scripts 不执行、外部源码不入图。
  - [x] Property/CAS：输入排列、重复语法、区域设置、连续 rebuild、源码/config/extends/package/lockfile/ignore/bootstrap/base revision 分别变化；验证确定性、去重、旧 batch 丢弃、有界重排、`detectedAt` 变化不单独推进 revision。
  - [x] Replacement：删除/移动源码或移除 import 后，旧 source Evidence 与唯一支撑边消失；partial/failed 不按缺失删除，共享外部节点生命周期仍不越过 Story 1.7。
  - [x] SQLite/Process：absent/v1/v2→v3、v3 reopen、未来 schema、事务内表重建/回填校验、Evidence 外键/ownership、fault injection、WAL 第二读者、重启恢复、真实构建产物 Worker + Named Pipe/UDS rebuild、malformed Worker response/超时/异常、shutdown/取消、单次原子 revision 切换和无绝对路径/源码泄漏。
  - [x] 回归 Story 1.1–1.4 与 1.19：执行 `pnpm type`、`pnpm lint`、`pnpm unit`、`pnpm build`、`pnpm contract`、`pnpm dependency-boundary`、`pnpm basic-security`、`pnpm planning-trace`、新 gate、`pnpm architecture-required` 和 `git diff --check`。

### Review Follow-ups (AI) — Attempt 1

- [x] [high] 由 graph-service 显式传递权威 `tsconfig.json`/`jsconfig.json` 入口，Worker 不再按文件名排序猜测配置。
- [x] [high] 使用 TypeScript 公开配置 API 安全处理 JSONC、package extends 与数组 extends，并递归封口实际配置依赖。
- [x] [high] 封口 project references，建立 source→project compiler options 映射并按文件使用所属项目配置。
- [x] [high] `rootDirs` 仅规范化与保首次去重，保留声明顺序。
- [x] [high] 按 AST import/require/dynamic usage 将公开 `ResolutionMode` 传给 `ts.resolveModuleName()`。
- [x] [high] 对齐 AD-21 的四种 language、三档 confidence；literal `import()` 固定为 dynamic/low，并贯通 SQLite v3。
- [x] [high] metadata broker 使用 no-follow 打开、realpath containment、读取前后文件身份/hash 绑定，允许合法 pnpm 硬链接与 nested `node_modules`。
- [x] [high] 4096 预算只统计唯一、非 manifest/config 的 resolution metadata candidates。
- [x] [high] `paths` 候选先结合 `baseUrl`/配置目录解析到受 containment 校验的工作区逻辑路径，并保留顺序与 wildcard。
- [x] [high] 补齐声明级 local export seed，覆盖 variable/function/class/type/interface/enum/default，并区分 `export =`。
- [x] [high] 区分真正 unresolved 与 resolved-but-invalid package metadata；后者只生成诊断，不伪造 unresolved purl 边。
- [x] [high] SQLite 提交与重启校验 module edge 端点/qualifier、Evidence source/edge 绑定及每条模块边的有效支撑。
- [x] [medium] `import {} from` 与 `export {} from` 保留 declaration-level 静态模块请求。
- [x] [medium] 对 `#imports`、URL、非法 `node:`、空字符串、残缺 scoped name 实施封闭 npm specifier 校验与稳定诊断。
- [x] [medium] Worker 按请求类型校验 `ok:true` payload，使用封闭 Analyzer 错误码，并由 runtime 映射为 `GRAPH_SCAN_FAILED`。
- [x] [medium] Worker 复用精确 `closePromise`、等待 terminate Promise；runtime 将 active run 与 Analyzer close 纳入同一关闭期限。
- [x] [medium] v2→v3 migration 在取得 `IMMEDIATE` 锁后重读版本并按当前状态分支。
- [x] [medium] v3 通过 PRAGMA/sqlite_master 校验精确列、CHECK、UNIQUE/index、FK 与毫秒 UTC `detected_at` 合同。

### Review Follow-ups (AI) — Attempt 2

- [x] [high] 修复 `ParseConfigHost.readDirectory` 对 `include`/`exclude`/`depth` 的忽略，确保同目录 referenced projects 只接收各自配置范围内的源码。
- [x] [high] 为 NodeNext `.ts` 静态 import 提供 package.json 推导的 `impliedNodeFormat`，避免错误使用 `require` 条件。
- [x] [high] 将 package metadata 查找限制在目标/package 边界内，禁止内部 JSON 或缺 manifest 外部包继承根 package 身份。
- [x] [high] 在无 tsconfig 或源码未归属项目等不完整上下文中，把已解析内部依赖置信度降为 `medium`。
- [x] [high] 对根外 project reference、`baseUrl`、`rootDir`、`typeRoots` 实施 containment fail-closed，不得静默删除后继续分析。
- [x] [high] 修复 watcher Worker 的依赖注入，使 README、node_modules、dist 等非语义变更不推进 commit fence 序列。
- [x] [high] 以有界可复用的增量 Program/Language Service 状态替代每请求一次性 Worker 与大输入重复 structured clone。
- [x] [high] 为 module target resolution 建立可复用的有界 manifest 索引，消除 relation×manifest 扫描。
- [x] [high] 为 SQLite ownership rows 建立一次性分组索引，消除 owner×ownership 扫描。
- [x] [medium] 深度校验 Worker `ok:true` payload，把嵌套非法关系映射为 `ANALYZER_PROTOCOL_INVALID`。
- [x] [medium] 正确生成文件级 namespace export seed，不递归提升 namespace 内部成员。
- [x] [medium] 强化 SQLite v3 精确 DDL/CHECK 校验，并在运行时封闭 relation/provenance/evidence kind 词汇。
- [x] [medium] 在 `001-bootstrap.ts` 写锁内重读 absent/v1 schema 状态，保证并发 migration 幂等。
- [x] [medium] 为合法空字符串与孤立代理项 ModuleExportName 提供可逆安全序列化，不终止 Worker。
- [x] [medium] 实施严格 SemVer 校验，损坏 package metadata 只诊断、不生成 high versioned purl。
- [x] [medium] 排除被参数或局部变量遮蔽的 `require` 调用，避免生成伪模块关系。

### Review Follow-ups (AI) — Attempt 3

- [x] [AI-Review][high] 为持久 Worker 建立项目级可复用的 TypeScript 增量 Program/Language Service（或等价增量状态），并以字节/事实数量双预算限制缓存；用计数断言证明跨请求复用和总内存上界。
- [x] [AI-Review][high] 修正 `worker-analysis.ts` 的 wildcard 目录 exclude/include/depth 语义，使 `**/generated` 排除目录及全部后代，并覆盖 referenced project 与深度边界。
- [x] [AI-Review][high] 缺失 package extends 必须 fail-closed 或输出封闭诊断；不存在候选、后续创建与 rename 必须进入 read set、watcher semantic sequence 和 commit fence。
- [x] [AI-Review][high] 将已封口的任意命名 consulted metadata 同步注入 watcher Worker，使最终 hash 到 SQLite COMMIT 窗口内的变更不能穿透。
- [x] [AI-Review][high] 对已解析到 root 内非 manifest 文件的 bare alias fail-closed/诊断，禁止伪造 `pkg:npm/<alias>@unresolved`。
- [x] [AI-Review][high] 将 resolution candidate 收集改为 Worker 与主进程两侧均流式早停的数量/字节有界实现，禁止完整物化、克隆、去重和排序后才拒绝。
- [x] [AI-Review][high] 为 ModuleResolutionHost 建立可复用目录索引与解析缓存，消除 `directoryExists/getDirectories` 和 observe/analyze 的 relation×file 重复解析，并加入访问次数复杂度断言。
- [x] [AI-Review][high] 为 SQLite module-edge Evidence 支撑查询增加确定性关联索引/查询方案，同步 exact DDL、索引 allowlist、迁移/恢复/提交测试，并以 EXPLAIN/规模断言证明非二次。
- [x] [AI-Review][medium] 分离 package version 缺失状态与合法 SemVer；任何 manifest version（包括字符串 `unresolved`）均严格校验，非法 metadata 只诊断且不生成 high versioned purl。
- [x] [AI-Review][medium] 将 absent/v1/v2/v3 全迁移路由放入同一写锁/事务内重读，保证并发 opener 幂等且全部成功。
- [x] [AI-Review][medium] SQLite 完整性检查显式验证 Evidence.workspaceKey、edge/source 所属 workspace 与生命周期一致，拒绝跨 workspace 污染。
- [x] [AI-Review][medium] Worker payload 深校验封闭 relationType 与 qualifier 判别联合；非法组合映射 `ANALYZER_PROTOCOL_INVALID`。
- [x] [AI-Review][medium] 为 for/for-in/for-of 头部的 `let`/`const require` 建立正确词法作用域，覆盖循环前、中、后的真实 require。
- [x] [AI-Review][medium] 为 Analyzer 配置/broker 普通错误定义正确失败分类与错误码；候选预算、metadata 读取和 broker 未稳定必须 fail-closed、标记 stale 且语义可观察。
- [x] [AI-Review][medium] 以节点/字节/深度预算的迭代闭包替代固定八轮 metadata broker；预算内深链成功，环和超预算确定性失败。
- [x] [AI-Review][low] `detectedAt` 只接受唯一规范 RFC3339 UTC millisecond 形式，拒绝 `24:00`、偏移和任何可规范化为其他字符串的值，并覆盖 domain/application/SQLite 边界。

### Review Follow-ups (AI) — Attempt 4

- [x] [AI-Review][high] CR4-001：安全探测 root 内、manifest 外的真实源码候选，将其封口为 non-manifest/blocked 状态；resolver 仅生成 `MODULE_RESOLUTION_FAILED` 诊断，不得生成 npm edge/Evidence，并纳入 watcher 与 COMMIT fence。
- [x] [AI-Review][high] CR4-002：为普通模块解析候选建立独立、有界的 `absentResolutionFiles`，与 required missing config 分离，并注入 watcher 与提交前后不存在性校验。
- [x] [AI-Review][high] CR4-003：让 composite rebuild 所需 `allNodes`、`allEdges`、`allEvidence`、`ownedSlices` 成为必填 committed snapshot 合同，删除 hierarchy-only 回退。
- [x] [AI-Review][high] CR4-004：校验 Worker 成功 target 的 host 语义；builtin 必须是规范 Node builtin，internal-file 必须绑定本轮 manifest 的逻辑路径与最终 ID，非法 payload 映射 `ANALYZER_PROTOCOL_INVALID`。
- [x] [AI-Review][high] CR4-005：metadata watcher 关闭前以 `AbortError` 结算全部 pending 请求；请求接入 signal/timeout，处理 `postMessage` 同步失败并覆盖 ACK 竞态。
- [x] [AI-Review][high] CR4-006：为 unresolved bare package 回退预建 package root 首候选索引，使 relation 查询保持 O(1) 或 O(logF)，并增加计数复杂度断言。
- [x] [AI-Review][high] CR4-007：冻结配置后预建 source→selected project/options/configurationComplete 映射，所有调用点 O(1) 复用并增加规模断言。
- [x] [AI-Review][high] CR4-008：metadata 异步读取与同步 COMMIT fence 统一使用 `MAX+1` sentinel 的有界分块读取，超限立即停止且不完整分配。
- [x] [AI-Review][high] CR4-009：为单次 Worker diagnostics、relations、localExportBindings 增加事实数量与估算 structured-clone 字节预算，稳定返回 `ANALYZER_RESOURCE_LIMIT` 并覆盖量化边界。
- [x] [AI-Review][high] CR4-010：将 SQLite CHECK normalizer 改为 quote-aware，只规范引号外关键字与排版，精确保留 literal 大小写和内容，并覆盖篡改 schema reopen/integrity。
- [x] [AI-Review][medium] CR4-011：定义封闭 typed Worker errors，精确映射 `CONFIG_INVALID`、`RESOURCE_LIMIT`、`PROTOCOL_INVALID` 与 `EXECUTION_FAILED`。
- [x] [AI-Review][medium] CR4-012：metadata 依赖深度只在发现新增层时消耗；允许第 128 层后的不耗深度稳定确认，环与超预算仍确定失败。
- [x] [AI-Review][medium] CR4-013：type-only import/import-equals 的 `require` binding 不得注册为值遮蔽，并覆盖 default、named、mixed 与 import-equals。
- [x] [AI-Review][medium] CR4-014：以确定性保守估值覆盖 Program、SourceFile 与目录索引内存，并增加对象数量上限和量化回归。
- [x] [AI-Review][low] CR4-015：更新仓库布局文档，改为在数量、字节与深度预算内迭代至稳定，不再描述固定八轮算法。

### Review Follow-ups (AI) — Attempt 5

- [x] [AI-Review][high] CR5-001：区分 consulted/blocked 同步 COMMIT fence 的 2 MiB/10 MiB 单文件上限，并覆盖 2 MiB、2 MiB+1、10 MiB、10 MiB+1 边界。
- [x] [AI-Review][high] CR5-002：为 blocked resolution 增加文件数与总字节预算、stat 前置预扣和流式 SHA-256；事务内只执行已准备好的有界低成本身份复核。
- [x] [AI-Review][high] CR5-003：绑定 Worker 成功 payload 的 source/language/confidence/target 语义，internal 由 logical path 推导 ID，external 由领域构造器重算规范身份，协议错误稳定映射 `ANALYZER_PROTOCOL_INVALID`。
- [x] [AI-Review][high] CR5-004：Program/SourceFile 预算覆盖 AD-19 的 50 package/5000 文件标准规模及必要 lib，并在 group 循环和 `CompilerHost.getSourceFile` 中以 MAX+1 增量早停。
- [x] [AI-Review][high] CR5-005：diagnostics/relations/bindings/targets/clone bytes 使用统一有界 sink 与增量保守估值，MAX+1 立即终止且不完整序列化超预算对象。
- [x] [AI-Review][high] CR5-006：以最小 SQL tokenizer/parser 只识别 quoted/comment context 外且 token 边界合法的 CHECK，并覆盖 line/block comment、双引号与嵌套表达式篡改负例。
- [x] [AI-Review][high] CR5-007：nested `node_modules` 缺 package.json 时按 containing file 的 Node ancestor lookup 顺序查询 package root fallback，保持 O(目录深度)。
- [x] [AI-Review][medium] CR5-008：让 `GraphStorePort` 返回必填 `CommittedCompositeGraphSnapshotV1`，删除 hierarchy-only 的类型合法形状。
- [x] [AI-Review][medium] CR5-009：恢复与提交入口均强制每条 Evidence 只有一个 `source:typescript:<sourceFileId>` owner，拒绝跨 slice 重复 Evidence。
- [x] [AI-Review][medium] CR5-010：SQLite exact index 改用 `index_xinfo`，锁定 BINARY、`desc=0`、`key=1` 与列顺序，并覆盖 NOCASE/DESC 负例。
- [x] [AI-Review][medium] CR5-011：配置解析复用一次性虚拟目录/扩展索引，消除每 project 对全 files 的重复 filter/sort，并加入线性访问量断言。
- [x] [AI-Review][medium] CR5-012：对 consulted、blocked、absent、absentResolution 四个集合统一拒绝 `rules.yaml`，稳定分类为 `ANALYZER_CONFIG_INVALID` 或安全诊断。
- [x] [AI-Review][medium] CR5-013：递归解包 Parenthesized/As/TypeAssertion/NonNull/Satisfies 后识别 default export 的真实 local identifier。
- [x] [AI-Review][medium] CR5-014：值遮蔽收集排除 declare/ambient declaration，覆盖顶层、namespace ambient 与普通 const/parameter 正反例。
- [x] [AI-Review][medium] CR5-015：Story 1.5 不生成 shared external/builtin node deletes，并把保留孤立共享节点纳入目标 snapshot/digest；最后引用回收留给 Story 1.7。
- [x] [AI-Review][medium] CR5-016：SQLite 恢复与 targetGraphDigest 校验各 kind 唯一 canonical `payload_json`，覆盖 hierarchy/Evidence/外部节点篡改负例。
- [x] [AI-Review][medium] CR5-017：大小写不敏感文件系统对动态 metadata watcher key 一致 case-fold，覆盖 `Configs/Base.json` 与 `configs/base.json` 事件。
- [x] [AI-Review][medium] CR5-018：Story verifier 纳入 index-read-set、sqlite-graph-store，并用 contract 配置单独运行真实 graph-service process；增加 verifier manifest 合同防止静默缩小。

### Review Follow-ups (AI) — Attempt 6

- [x] [AI-Review][high] CR6-001：让虚拟 TypeScript host 遵循实际文件系统大小写语义，并把 Windows/macOS 大小写差异解析结果规范回 manifest casing；当前 `src/dep.ts` + `import "./Dep.js"` 在 Windows 被误报 unresolved 并漏掉内部依赖边。
- [x] [AI-Review][high] CR6-002：修复带点目录的 tsconfig wildcard exclude 语义；`**/.generated`、`**/generated.v2` 必须排除目录后代，不能因末段含 `.` 被当成文件 pattern 而把源码错误归入该 project 配置。
- [x] [AI-Review][high] CR6-003：主线程 workspace change 分类必须与 watcher Worker 共用大小写折叠 key；Windows 上已 consulted 的 `configs/base.json` 收到 `Configs/Base.json` change 时必须立即推进 generation、发布并持久化 stale。
- [x] [AI-Review][high] CR6-004：调整持久 Program 缓存预算/分层，使 AD-19 的 50 project/5000 file 标准规模仍能跨请求复用；当前连续两轮得到 `projectStateCount=0`、`programBuilds=50→100`、`programReuses=0`，退化为每请求全量重建。
- [x] [AI-Review][high] CR6-005：Windows deadline 清理不得用根 child 的 exit/close 证明整棵进程树收敛；必须等待 `taskkill /T /F` 或等价后代级验证完成，并在树清理失败时返回 `EPROCESSCLEANUP`。
- [x] [AI-Review][high] CR6-006：project/program 数量预算必须在第 `MAX+1` 个配置进入队列或解析前后立即早停；当前会先解析、保存并执行全部 source membership，再在 Program 创建前拒绝超限输入。
- [x] [AI-Review][high] CR6-007：Worker 成功 payload 的 diagnostic、binding、relation 范围必须绑定所属源码的 UTF-16 长度；长度 1 的源码当前可接受 `{start:0,end:Number.MAX_SAFE_INTEGER}` 并生成越界 Evidence。
- [x] [AI-Review][medium] CR6-008：主线程必须由 resolution kind、项目完整性与 target 状态重算/校验 confidence；当前静态规范 `node:fs` 或 resolved npm target 携带 `low|medium` 仍可通过 Worker 成功协议并持久化。
- [x] [AI-Review][medium] CR6-009：unresolved npm subpath 校验不得拒绝 Node 合法且无逃逸语义的字符；当前 `pkg/feature+debug` 被误判 `MODULE_SPECIFIER_INVALID`，没有生成 AC4 要求的 medium `pkg:npm/pkg@unresolved`。
- [x] [AI-Review][medium] CR6-010：把 `NoSubstitutionTemplateLiteral` 作为确定性的字符串 literal 处理；当前使用无替换模板参数调用 `import()` 与 `require()` 时会被误报非 literal，并漏掉可精确提取的关系。
- [x] [AI-Review][medium] CR6-011：`runProcessWithDeadline` 必须拒绝超过 Node 定时器上限的 `timeoutMs` 与 `killGraceMs`；当前 `3_000_000_000` 会被 Node 收缩为约 1 ms，导致正常进程立即超时或清理阶段过早失败。
- [x] [AI-Review][medium] CR6-012：Analyzer `requestTimeoutMs` 必须限制在 Node 可表达的定时器范围；当前超范围正安全整数可通过构造校验，并让所有 Worker 请求约 1 ms 即超时、销毁实例。
- [x] [AI-Review][medium] CR6-013：源码字节解码应兼容 TypeScript 公共读取边界支持的 UTF-16LE/BE BOM，或以明确的受支持编码合同在 scanner 前置拒绝；当前任一 UTF-16 BOM `.ts/.js` 会使整个 Analyzer Job 以非法 UTF-8 失败。

### Review Follow-ups (AI) — Attempt 7

- [x] [AI-Review][high] CR7-001：修复卷大小写语义探测在路径各组件都不含 ASCII 字母时的误判；`apps/graph-service/src/workspace-scanner.ts:47-65` 在 Windows/macOS 大小写不敏感卷上对如全数字或全非 ASCII indexing root 会回退为 `true`，使 Analyzer、resolver 和 watcher 全部采用错误的大小写敏感语义。
- [x] [AI-Review][high] CR7-002：将 host 的 `caseSensitiveFileNames=false` 完整传播到虚拟目录键、目录枚举、扩展名与 include/exclude glob；`packages/adapters/analyzer-typescript/src/worker-analysis.ts:853-906,1077-1109,1395-1469` 仍按原始大小写查找，可在大小写不敏感卷上把实际存在的内部依赖误判为 unresolved，或把本应属于 project 的源文件放入 `<unconfigured>`。
- [x] [AI-Review][high] CR7-003：将 `pnpm-workspace.yaml` 纳入 Analyzer 根 metadata 封口与 watcher 语义事件；`apps/graph-service/src/analyzer-config.ts:490-497` 和 `apps/graph-service/src/index-read-set.ts:1048-1054` 同时遗漏该 workspace manifest，修改它既不改变 `configDigest`，也不推进 stale/generation，违反 AC2/Task 3 对 package/workspace manifest 的要求。
- [x] [AI-Review][high] CR7-004：调整持久 Program 状态的 24 MiB 单状态上限或分层计量，使 AD-19 的 50 project/5000 file/500000 LOC 标准规模仍能跨请求复用；`packages/adapters/analyzer-typescript/src/worker-analysis.ts:956-1065,1211-1214` 对全部原始字节、SourceFile 文本/对象和 Program 合并估值后直接不缓存，而现有标准规模回归每文件只有一行，未覆盖 500000 LOC。
- [x] [AI-Review][high] CR7-005：Windows 进程树清理不得把 `taskkill /T /F` 的 exit code 128 无条件视为树已收敛；`scripts/ci/run-process-with-deadline.mjs:221-225` 在根进程已退出但脱离后代仍存活时可提前返回 pass，继续留下孤儿进程并违反 CR6-005 的后代级收敛合同。
- [x] [AI-Review][high] CR7-006：修正 Story blocking verifier 的 clean-checkout 构建顺序；`scripts/ci/verify-typescript-module-analysis-v1.mjs:33-40` 首先执行 `@codegraph/application` 的 `tsc -p`，但其 project reference 依赖的 `@codegraph/domain/dist` 未被先构建且 `dist/` 被忽略，独立 blocking gate 在干净 checkout 会在首个构建步骤失败。
- [x] [AI-Review][medium] CR7-007：区分“根本不存在的 bare package”与“已存在 manifest 但 `exports`/子路径规则使 TypeScript 解析失败”；`packages/adapters/analyzer-typescript/src/worker-analysis.ts:1541-1562` 只要看到 `package.json` 就删除 manifest-less fallback，后续会把后一种情况静默降为 `pkg:npm/...@unresolved`，而不是按 Task 6 为无效 package metadata/子路径解析失败输出稳定诊断并拒绝伪边。

### Review Follow-ups (AI) — Attempt 8

- [x] [AI-Review][high] CR8-001：以符合真实宿主文件系统的 Unicode 路径等价语义替换 `toLocaleLowerCase("en-US")` key，并在 VirtualFileMap、VirtualDirectoryIndex、glob/containment 与 watcher 共用；`packages/adapters/analyzer-typescript/src/worker-analysis.ts:947-950,1304-1310,1340-1343,1832-1834` 与 `apps/graph-service/src/index-read-set.ts:174-177` 当 `caseSensitiveFileNames=false` 时把 JavaScript locale lowercase 当作 NTFS/APFS case-fold，但真实 Windows 卷上互异的 `İ.ts` 与 `i\u0307.ts` 会被合并为同一 key 并触发“大小写冲突路径”，反向 Unicode 对也可把宿主视为同一的路径分开，导致合法 Analyzer 输入失败、内部 import 漏解析或 watcher stale/COMMIT fence 漏推进。最小修复方向是建立经 Windows/macOS 非 ASCII 回归验证的单一 host path identity key，禁止继续以 locale lowercase 近似。
- [x] [AI-Review][high] CR8-002：将宿主大小写语义传入全部 `node_modules` 段判断、package root/fallback index、metadata broker 与目标分类；`apps/graph-service/src/analyzer-config.ts:346-376,819-822`、`packages/adapters/analyzer-typescript/src/worker-analysis.ts:1595-1605,1641-1654,1769-1785` 与 `packages/adapters/analyzer-typescript/src/module-target-resolver.ts:55-82,157-160` 只精确匹配小写 `node_modules`。在大小写不敏感卷上，`caseSensitiveFileNames=false` 且 manifest/metadata casing 为 `NODE_MODULES`/`Node_Modules` 时，纯内存复现会把外部文件误标 high `internal-file`，或把 `exports` 拒绝子路径伪造为 medium unresolved npm 边。最小修复方向是所有边界检查共用 CR8-001 的 host key，同时保留 manifest 原始 casing。
- [x] [AI-Review][high] CR8-003：让 Story blocking verifier 在 clean checkout 中自给构建其合同测试需要的 `@codegraph/service-client`；`scripts/ci/verify-typescript-module-analysis-v1.mjs:10-18` 的 `buildFilters` 遗漏该包，但 `tests/contract/graph-service-process.test.ts:432-436` 随后强制 `access("packages/service-client/dist/index.js")`。`dist/` 未被 Git 跟踪，其他 filter 也不会构建该包，因此清洁检出必然在进程合同失败，VALIDATION15 的通过依赖当前残留产物。最小修复方向是按拓扑加入 service-client build filter，并由 verifier manifest 合同锁定该前置。
- [x] [AI-Review][high] CR8-004：Windows 后代收敛不得依赖 root 退出后的单次当前 CIM 父子快照；`scripts/ci/run-process-with-deadline.mjs:226-229,293-311` 只能从 root PID 沿快照中仍存活的 `ParentProcessId` BFS。真实 Windows 复现中，root 等待 intermediate 退出，intermediate 创建 detached grandchild 后先退出，root 随后退出且 `taskkill` 返回 128；调用方错误返回 `status=pass, termination={code:0,kind:"exit"}`，1.6 秒后 grandchild 仍成功写入 marker。最小修复方向是在祖先存活时保存完整进程树，或改用 Windows Job Object/等价可跟踪树级机制，禁止用已断链的单次快照提供收敛证明。
- [x] [AI-Review][medium] CR8-005：完整实现 AD-3 固定的 `workspacePackages:[{root,name}]` v1 合同；`packages/application/src/indexing/analyzer-config-snapshot.ts:11-14,193-195` 的公共类型和规范化只保留 `root`，即使运行时传入 `name` 也会在 snapshot/digest 前被丢弃，使同 root、不同 package name 产生相同语义摘要，Story 1.9 启用时只能破坏 v1 合同或遗漏 package 身份。最小修复方向是在当前封闭类型、规范化、排序、digest 和合同测试中同时保留 `root` 与 `name`，生产值在 Story 1.9 前仍固定为 `[]`。
- [x] [AI-Review][medium] CR8-006：对齐 Node/TypeScript 的合法 npm package subpath 语义；`packages/adapters/analyzer-typescript/src/module-target-resolver.ts:167-203` 只允许 `[A-Za-z0-9._~+-]`，将不含逃逸语义的 `pkg/feature@debug`、`pkg/feature!debug` 等合法 bare subpath 误判为 `MODULE_SPECIFIER_INVALID`，而不是 AC4 要求的 medium `pkg:npm/pkg@unresolved`；TypeScript 6.0.3 的 `resolveModuleName()` 可将 `pkg/feature@debug` 解析到对应 `.d.ts`，当前 `resolveModuleTarget()` 却稳定返回 invalid 诊断。最小修复方向是仅拒绝空段、`.`/`..`、反斜线、绝对路径/URL 与编码逃逸，不再以过窄字符白名单限制安全子路径。

### Validation Follow-ups (AI) — Attempt 10

- [x] [blocker] VAL10-001：消除 `analyzer-config-capture.test.ts` 在代表性组合负载下的测试级 timeout，保持 blocked 预算、128 层稳定确认、50 project/5000 file 与 MAX+1 早停等量化语义不变，且不得增加 timeout、skip、only 或 retry。
- [x] [blocker] VAL10-002：精确移除 VALIDATION10 追加到根 `.gitignore` 的唯一 `.tmp-*/` 行，恢复与 integration base 字节一致，不读取或修改被隐藏的 unowned 临时目录。

### Validation Follow-ups (AI) — Attempt 11

- [x] [blocker] VAL11-001：保持 `startTimeoutMs=500`、`pollIntervalMs=5`、launcher 只发布 token 与连续三次 stale 的安全分类语义，通过测试专用确定性 discovery 注入隔离真实文件系统抖动，稳定返回 `SERVICE_INSTANCE_CONFLICT`。
- [x] [blocker] VAL11-002：保持 Windows 50ms deadline、600ms marker 与默认 10 秒 cleanup grace，通过独立进程组即时广播和 `taskkill /T /F` 的有界收敛证据完成后代进程树清理，清理失败显式返回 `EPROCESSCLEANUP`。

### Validation Follow-ups (AI) — Attempt 16

- [x] [blocker] VAL16-001：修复完整并行 unit 负载下短生命周期 Git 的 Windows Job helper 收敛；保持 30 秒 Git deadline、默认 10 秒 cleanup grace、45 秒测试上限与 Job Object 断链后代安全语义，不修改 `tests/unit/gate-applicability.test.ts`，不增加 retry/skip/only/todo，并让真实 Git 临时仓库通过正常 teardown 自动删除。

### Review Follow-ups (AI) — Attempt 9

- [x] [AI-Review][high] CR9-001：缺失的根级 Analyzer 元数据必须进入 absent CAS/read-set，封闭不存在与出现之间的提交竞态。
- [x] [AI-Review][high] CR9-002：嵌套 tsconfig 的 `readDirectory` 必须以正确 base path 支持 include 父级/兄弟目录源码，同时保持 indexing root、ignore 与预算边界。
- [x] [AI-Review][high] CR9-003：不完整 project context 下的 builtin/external 静态边必须按 AD-21 降低置信度并输出一致诊断。
- [x] [AI-Review][high] CR9-005：Worker 必须在完整解码输入和构建完整 AST 前执行确定性 admission/budget fence，超限稳定返回 `ANALYZER_RESOURCE_LIMIT`。
- [x] [AI-Review][medium] CR9-006：合法含冒号的 npm/package subpath 必须与 URL/协议和 Node builtin 正确区分，不得伪造 unresolved package。
- [x] [AI-Review][medium] CR9-007：虚拟目录后代枚举不得使用遗漏 U+FFFF 文件名的 prefix upper-bound，并继续保持确定性与预算受控。
- [x] [AI-Review][medium] CR9-008：resolution candidate 遇到祖先普通文件导致的 `ENOTDIR` 时应按候选不存在处理，真实权限、I/O 与竞态错误仍须 fail-closed。

## Dev Notes

### Developer Context

- Story 1.5 是 `sprint-status.yaml` 按文件顺序发现的第一个 `backlog` Story；Epic 1 已为 `in-progress`。
- 唯一直接前置是 Story 1.19，而不是数值上相邻的 Story 1.4。Story 1.19 已交付唯一 mutation channel、完整 read-set/CAS、幂等 GraphPatch、原子 revision 和半提交不可见；本 Story 只能在这条主干上增加 source analyzer。
- `packages/adapters/analyzer-typescript/src/index.ts` 当前仍是空壳；仓库已经锁定 TypeScript 6.0.3，但该 adapter 尚未声明 TypeScript 运行时依赖。
- 当前 domain/application/store 事实模型高度 hierarchy 专属，SQLite v2 的 `nodes.kind` 仅允许 workspace/directory/file，`edges.relation_type` 仅允许 contains，edge UNIQUE 也未包含 qualifier；必须通过新增 v3 migration 和最小核心泛化解决。
- 当前工作树有与本 Story 无关的未跟踪临时目录和 `想法.md`。不得删除、移动、修改、加入 Story File List 或使用 destructive Git 命令处理。

### Scope Boundaries

本 Story 明确不实现：

- Story 1.6：`BasicSymbolV1`、本地 symbol 实体、导航范围和 local export→symbol 最终边。
- Story 1.7：Evidence 去重键的完整持久执行、冲突仲裁、last-active 删除、partial tombstone、跨来源 ownership 与删除语义。
- Story 1.8：≥500 标注声明准确率 corpus、micro-F1/high-confidence precision 判定和失败样本发布。
- Story 1.9：npm/Yarn/pnpm workspace discovery、workspace-package 节点、跨 package 聚合与 degraded summary。
- Story 1.10–1.13：`.codegraphignore` grammar、last-valid、invalid UTF-8、watcher/reconciliation 生命周期与配置诊断完善。
- 保存后增量更新、公共查询/doctor/CLI/UI、循环投影、Findings、impact、export、release、`references`、调用图、成员/局部符号或运行时依赖。
- TypeScript 7、Tree-sitter、LSP、SCIP/CPG、第二解析器、第二存储、ORM、通用队列框架或无责任边界的 utils/common/helpers 包。

### Architecture Compliance

- **依赖只向核心收敛。** domain/application 定义事实与端口；analyzer-typescript/store-sqlite 实现适配器；graph-service 是唯一组合根。Analyzer、SQLite、Worker 或 TypeScript 类型不得渗入核心。[Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-1依赖只向核心收敛`]
- **唯一原子主干。** Analyzer 只输出 FactBatch，application 计算 GraphPatch，服务经唯一 snapshot mutation channel 提交；客户端和 Analyzer 都不写共享状态。[Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-3图谱只通过原子-GraphPatch-变更`]
- **稳定身份。** 内部 file 使用工作区作用域 `cg://`，外部 npm 使用 purl，Node built-in 使用 `node:`；路径使用 NFC + 相对 POSIX，范围使用 0-based UTF-16 半开区间。[Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-4确定性实体-ID-与证据分层`]
- **单一权威解析器。** TypeScript 6.0.3 稳定 Compiler API 是 TS/JS 首实现唯一事实源，禁止并行解释同一依赖事实。[Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-5-TypeScript-Compiler-API-是-TSJS-权威分析源`]
- **唯一语法映射。** 所有 import/export/re-export/type/value/dynamic/import-equals 规则必须逐字服从 AD-24；不得由 adapter、application 和客户端分别解释。[Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-24-TSJS-语法到规范关系的映射唯一`]
- **渐进式 CI。** 模块分析首次公开进入 rebuild 时，同一 PR 必须增加真实可失败 blocking gate；公共 RPC/Schema 若变化，再按实际 diff 一能力一 gate。[Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-28能力首次落地即进入渐进式-CI`]

### Analyzer Config and Digest Contract

`AnalyzerConfigSnapshotV1` 的规范语义形状：

~~~json
{
  "version": 1,
  "analyzerKind": "typescript",
  "analyzerVersion": "6.0.3",
  "effectiveCompilerOptions": {},
  "consultedFiles": [
    { "path": "tsconfig.json", "contentHash": "sha256-hex" }
  ],
  "effectiveIgnore": {
    "version": 1,
    "effectiveDigest": "sha256-hex"
  },
  "workspacePackages": []
}
~~~

- `configDigest`：对上述对象执行 RFC 8785 JCS → UTF-8 → SHA-256 小写十六进制。
- `inputDigest`：对 `{version:1, analyzerKind, configDigest, inputs:[{path,contentHash}]}` 执行相同算法；inputs 按规范 path 升序。
- ignore generation、bootstrap generation、statusEpoch 和 baseGraphRevision 是并发栅栏，不进入语义 digest；完整 EffectiveIgnoreSnapshotV1 仍整体参与提交 CAS。
- `rules.yaml`、Job ID、时间、SQLite 顺序、Worker ID、宿主分隔符和区域设置都不得进入 digest。

### Canonical Syntax and Target Matrix

| 输入 | 规范关系/qualifier | 目标或结果 |
| --- | --- | --- |
| `import x from "./x"`、side-effect/default/namespace import | `imports:value` | 按固定目标优先级解析 |
| `import type`、specifier `type`、type import-equals | `imports:type` | 按固定目标优先级解析 |
| `require("x")` | `imports:value` | 仅字符串 literal |
| `import("x")` | `imports:dynamic` | 仅字符串 literal；非 literal 不建精确边 |
| external import-equals | `imports:type|value` | EntityName 内部别名不建边 |
| `export {a as b} from "x"` | `imports:type|value` + `exports:reexport:b:a:type|value` | file→target module entity |
| `export * from "x"` / `export type *` | imports + `exports:star:value|type` | 每 declaration 一组 |
| 本地 named/default export | 仅保留规范化描述 | Story 1.6 建 local symbol 与最终 exports 边 |
| Node built-in（含 bare `fs`） | 目标 ID `node:<module>` | 独立 node-builtin，不伪装 npm 包 |
| root 内 TypeScript resolved file | 现有 file `cg://` ID | 不新建 module 节点 |
| root 外 resolved npm package | 标准 npm purl | 最近 package.json 的 resolved version |
| unresolved bare | `pkg:npm/...@unresolved` + medium | 可建低置信依赖 |
| unresolved relative/absolute | 无 edge，稳定诊断 | 不创建假文件节点 |

### Fact Ownership and Lifecycle Boundary

| 事实 | Owner / 维持方式 | Story 1.5 持久化 | Story 1.5 删除责任 | 后续 Story |
| --- | --- | --- | --- | --- |
| workspace/directory/file/contains | `hierarchy:<indexingRootId>` | 是，复用既有事实 | hierarchy complete replacement | 已由 1.19 建立 |
| TypeScript Evidence | `source:typescript:<fileId>` | 是 | complete slice replacement；文件消失时退休 slice | 1.7 扩展冲突/tombstone/通用生命周期 |
| imports/exports 规范 edge | 由 active Evidence 维持，不归单一 source 独占 | 是 | 本 Story删除零 Evidence 的模块边 | 1.7 扩展多 producer/跨来源规则 |
| external-package/node-builtin | 由规范 edge 引用维持 | 是 | 本 Story不得误删仍被引用节点；最后引用回收可由 1.7 完整化 | 1.7 |
| `LocalExportBindingSeedV1` | Analyzer/application 内部交接事实 | 不写 graph edge | 随 source slice 重算 | 1.6 提升为 symbol/exports edge |
| workspace-package/depends_on | Story 1.9 的 manifest/package/projection 合同 | 否 | 无 | 1.9 |

### Current Repository State and Required Updates

| 路径 | 当前状态 | 本 Story 变化 | 必须保持 |
| --- | --- | --- | --- |
| `packages/adapters/analyzer-typescript/src/index.ts` | 空壳，只声明职责 | 增加 AnalyzerPort 实现、Worker 边界、TS6 配置/语法/解析适配 | 不访问 GraphStore；不加载 plugin/scripts；中文 JSDoc |
| `packages/adapters/analyzer-typescript/package.json` | 仅依赖 application/domain | 精确增加 `typescript: 6.0.3` | adapter role；不引入 Tree-sitter/LSP/SCIP |
| `packages/domain/src/graph-identity.ts` / `hierarchy.ts` / `graph-patch.ts` | 仅 hierarchy 节点、contains、HierarchyFactBatch/GraphPatch | 增加封闭 module target/node/edge/fact 模型并泛化 patch | 已有 hierarchy ID、方向和 replay 结果不变 |
| `packages/application/src/indexing/*` / `ports/*` | 只构建 hierarchy batch/patch 与 GraphStorePort | 增加 AnalyzerPort、配置快照、source batch 规范化与复合 patch | application 不读 fs/TS/SQLite；唯一 commit 端口 |
| `apps/graph-service/src/workspace-scanner.ts` | 安全扫描、hash、物理身份与预算 | 为 Analyzer 安全输入/consulted files 提供必要受限边界 | realpath/no-follow/预算/取消/最终 fence 不回退 |
| `apps/graph-service/src/index-read-set.ts` | hierarchy manifest + ignore/bootstrap/base revision | 扩展 AnalyzerConfigSnapshot 与 consulted files CAS | generation 与语义 digest 继续分离；三次有界重排 |
| `apps/graph-service/src/index-job-runtime.ts` | scan→hierarchy batch→patch→原子提交 | 插入 Worker Analyzer 与 source module batch，组成单一原子 mutation | 单 Job/单 writer、旧 revision 可读、shutdown 有界 |
| `packages/adapters/store-sqlite/src/migrations/*` | v1/v2 精确八表；node/edge CHECK 仅 hierarchy | 新增 v3 migration，扩展 node/edge/evidence 合同 | 不回改历史 migration；不新增未来表 |
| `packages/adapters/store-sqlite/src/sqlite-graph-store.ts` | 只校验/提交 hierarchy complete patch并按 hierarchy 恢复 | 支持复合 hierarchy/source patch、模块事实与严格恢复回验 | 同步事务、WAL reader、fault rollback、未知 schema fail closed |
| `packages/adapters/store-sqlite/src/index.ts` | 导出 v1/v2 migration 与 store | 导出 v3 migration 和新增 store 能力 | 历史导出与打开流程兼容 |
| `scripts/architecture/check-dependency-boundaries.mjs` | adapter 外部依赖 allowlist 未含 TypeScript | 仅为 analyzer-typescript 精确允许 TypeScript | domain/application 仍禁止基础设施依赖 |
| `pnpm-lock.yaml` | TypeScript 仅由根工具链锁定 | 同步 analyzer runtime 的精确 6.0.3 依赖图 | frozen lockfile；不得漂移到 7.x |
| `ci/quality-gates.v1.yaml` / tests / scripts | 23 项现有 gate，含 Story 1.19 原子 gate | 增加独立模块分析 blocking gate；公共能力按实际 diff 处理 | registry 排序/digest/producer/evidence 流程不回退 |
| `docs/repository-layout.md` / `docs/protocol/service-control-v1.md` | 描述 hierarchy 职责和原子 rebuild | 更新 Analyzer Worker、模块事实职责与单 revision 可见性；仅记录真实协议变化 | 相对路径、唯一 writer、envelope/error 兼容 |

### Expected File Structure

下列是责任边界，不要求机械创建全部文件；只有出现独立职责时才拆分，禁止建立通用 utils/common/helpers 包。

~~~text
packages/domain/src/
  module-dependency.ts                 # 模块目标、关系、qualifier、Evidence 描述与诊断
  graph-identity.ts                    # 扩展 imports/exports 与外部身份
  graph-patch.ts                       # hierarchy/source 共用 patch 合同
packages/application/src/
  ports/analyzer-port.ts               # AnalyzerPort 与输入输出
  indexing/analyzer-config-snapshot.ts # 规范快照与 digest 输入
  indexing/module-fact-batch.ts        # source FactBatch 规范化
  indexing/graph-patch-builder.ts      # 单一复合 patch 主干
packages/adapters/analyzer-typescript/src/
  index.ts
  typescript-analyzer.ts               # TS6 稳定 API 实现
  analyzer-worker.ts                   # Worker 入口与消息校验
  module-syntax.ts                     # AD-24 唯一映射
  module-target-resolver.ts            # 唯一目标优先级
apps/graph-service/src/
  analyzer-config.ts                   # 受信任配置快照与 consulted files
  index-read-set.ts
  index-job-runtime.ts
packages/adapters/store-sqlite/src/migrations/
  003-module-dependencies.ts           # v3；仍保持八表
tests/unit/
  typescript-module-syntax.test.ts
  typescript-module-resolution.test.ts
  analyzer-config-snapshot.test.ts
  module-fact-batch.test.ts
tests/contract/
  typescript-module-analysis.test.ts
scripts/ci/
  verify-typescript-module-analysis-v1.mjs
~~~

### Previous Story Intelligence

- 按 create-story 数值规则，上一 Story 文件是 Story 1.4；它反复证明输入必须在副作用前 fail closed，物理路径与公共路径分离，扫描/关闭有 deadline，恢复必须交叉校验真实事实、摘要与 terminal Job。
- Story 1.4 的 review 修复集中在 root/目录/文件替换竞态、shutdown 取消、有界 close、SQLite 恢复不变量、兼容状态与 metadata+ready 双屏障。Analyzer 文件读取和 Worker 生命周期必须把这些作为回归基线。
- 前序实现统一使用 Red → Green → Refactor、真实 fault injection、真实进程/IPC、无 skip/todo/only、用户无关文件不触碰；本 Story继续沿用。

### Direct Dependency Intelligence: Story 1.19

- 唯一生产 mutation 入口是 `GraphStorePort.commitAtomicGraphUpdate()`；logical Job 在 `apps/graph-service/src/index-job-runtime.ts` 捕获 read-set、计算 patch、复核 fence 后提交。Story 1.5 不得新增第二入口。
- `configDigest/inputDigest` 只表达语义，ignore/bootstrap/base revision 只表达并发栅栏；AnalyzerConfigSnapshot 必须加入同一组合式 CAS，不能把 generation 混入 digest。
- no-op/replay 不推进 graphRevision；partial/failed/cancelled/CAS stale 不改变已提交图谱；旧 committed revision 在重排或失败期间持续可读。
- 现有 `GraphPatchV1`、SQLite validator 和恢复拓扑检查仍是 hierarchy 专属。应最小泛化并保留所有 1.19 测试，不得复制一套 source commit 函数。

### Git Intelligence

- `13e3bb7f`（`feat: complete deterministic rebuild story 1.19`）建立 FactBatch/GraphPatch、完整 read-set、migration v2、原子 revision、三次 stale 重排与专属 gate；本 Story直接扩展这些模式。
- `6d0c84c0`、`76fe1639`、`4c071ce7`、`2515a0a6` 体现的交付纪律是“小步修复 + 完整回归 + 最终候选证据闭环”；不要把“原子 SQLite 提交”误解为要求巨型 Git commit。
- 当前未跟踪临时目录与 `想法.md` 属于用户，不得纳入 Story 实施或提交。

### Latest Technical Information

- 2026-07-26 npm Registry 核验：`typescript@6.0.3` 可用；registry `latest` 为 7.0.2、`next` 为 7.1.0-dev，但 Architecture AD-5 明确锁定 6.0.3，本 Story不得升级或使用 7.x API。
- 本地 `typescript@6.0.3` 已验证公开存在 `createIncrementalProgram`、`createLanguageService`、`resolveModuleName`；Architecture technology review 也确认所需 AST guards、module resolution 与 Worker/Node 24 可实现。
- `typescript-eslint@8.64.0` peer range 为 `>=4.8.4 <6.1.0`，TypeScript 6.0.3 位于兼容范围；升级到 7.x 会越过当前 lint 工具链兼容边界。
- purl scoped npm package 必须遵守标准 `%40scope/name` 编码；未解析版本 `@unresolved` 是项目批准的低置信身份，不应替换为包管理器特定字符串。

### UX and Downstream Contract

- Story 1.5 不交付 UI，但必须让下游能够严格区分 internal file、external-package、node-builtin；Node built-in 不能伪装成 npm 包，外部目标不能携带假本地导航路径。
- 边必须保留方向、关系类型、来源、置信度与范围；低置信关系不能使用错误语义。图、列表、详情、屏幕阅读器和结构导出后续必须消费同一事实，不得客户端重算。
- “模块”是 directory/workspace-package 的用户可见逻辑聚合，不是新的持久实体类型；本 Story只生成规范文件模块关系与外部目标节点。

### Testing Standards

- 使用 Vitest 4.1.10；Worker、SQLite、IPC 测试必须是真实实现或职责等价的可控边界，不用内存 Map 冒充事务/迁移/进程证据。
- SYS-IDX-003/004 是本 Story的 P0 原子场景：唯一语法关系与固定目标解析优先级。SYS-IDX-005/006 的完整 Evidence/删除生命周期由 Story 1.7 收敛，但本 Story必须交付其输入元数据和 source slice 身份。
- 测试等待使用事件、状态、revision 或有界轮询，禁止硬编码 sleep 验证 Worker、quiet window、CAS 重排或 shutdown。
- 测试输出、诊断、日志和 fixture 不得包含真实 token、用户凭据、绝对本机路径或源码正文；临时 workspace/cache/DB/Worker/endpoint teardown 必须幂等。
- 所有 TypeScript/JavaScript 接口、类、方法与复杂逻辑使用符合 JSDoc 的中文注释；配置、脚本、测试说明和文档注释同样使用中文。

### References

- [Source: `../planning-artifacts/epics.md#Story-15提取模块依赖并解析目标`]
- [Source: `../planning-artifacts/epics.md#StoryDependencyDagV1权威`]
- [Source: `../planning-artifacts/epics.md#架构与实施附加要求`]
- [Source: `../planning-artifacts/prds/prd-bmad-2026-07-09/prd.md#FR-2提取-TypeScriptJavaScript-依赖关系`]
- [Source: `../planning-artifacts/prds/prd-bmad-2026-07-09/prd.md#FR-5图谱版本稳定-ID-与过期状态`]
- [Source: `../planning-artifacts/prds/prd-bmad-2026-07-09/prd.md#55-可演进性`]
- [Source: `../planning-artifacts/prds/prd-bmad-2026-07-09/addendum.md#2-技术边界输入`]
- [Source: `../planning-artifacts/prds/prd-bmad-2026-07-09/addendum.md#54-分析范围与正确性合同`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-3图谱只通过原子-GraphPatch-变更`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-4确定性实体-ID-与证据分层`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-5-TypeScript-Compiler-API-是-TSJS-权威分析源`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-21-Evidence-置信度和冲突处理固定`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/ARCHITECTURE-SPINE.md#AD-24-TSJS-语法到规范关系的映射唯一`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/IMPLEMENTATION-GUIDE.md#4-核心端口草图`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/IMPLEMENTATION-GUIDE.md#6-图谱更新实现`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/IMPLEMENTATION-GUIDE.md#8-TypeScript-分析器`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/IMPLEMENTATION-GUIDE.md#验证矩阵`]
- [Source: `../planning-artifacts/architecture/architecture-bmad-2026-07-13/reviews/review-technology-reality-round5.md`]
- [Source: `../planning-artifacts/ux-designs/ux-bmad-2026-07-13/EXPERIENCE.md#Trust-Provenance--Performance`]
- [Source: `../test-artifacts/test-design-qa.md#Indexing-Semantics--Querying14`]
- [Source: `1-4-安全初始化首次图谱与最小存储.md#Dev-Notes`]
- [Source: `1-19-完成确定性-rebuild-与原子提交.md#Dev-Notes`]
- [Source: `../../project-context.md#项目级编码约束`]

## Dev Agent Record

### Agent Model Used

GPT-5 Codex（Amelia）

### Implementation Plan

- RED：先建立模块事实合同、配置快照、Worker、AD-24、目标解析、复合 patch、SQLite v3 与真实进程失败用例。
- GREEN：按 domain → application → adapter → graph-service → store 主干实现，并保持 Analyzer 不访问 GraphStore。
- REFACTOR：统一 hierarchy/source 原子提交与确定性排序，补齐 replacement、恢复回验和 blocking gate。
- Review continuation attempt 2：先为 CR2 的 16 项 finding 建立可复现 RED，再以封闭配置、持久 Worker/增量缓存、一次性索引和精确 migration 校验完成最小修复。
- Review continuation attempt 3：严格按 16 项 CR3 finding 顺序逐项 RED→GREEN→REFACTOR；性能/内存项使用数量、字节、访问次数和查询计划的量化断言，保持唯一 Analyzer/GraphPatch/GraphStore 主干与 Story 1.6/1.7/1.9 边界。
- Review continuation attempt 4：严格按 CR4-001→CR4-015 顺序逐项 RED→GREEN→REFACTOR；新增 blocked/absent resolution read-set、host payload 语义校验与确定性资源预算，继续保持 graph-service 唯一组合根和单一原子 GraphPatch 主干。
- Review continuation attempt 5：严格按 CR5-001→CR5-018 顺序逐项 RED→GREEN→REFACTOR；预算与复杂度 finding 使用边界、访问次数、MAX+1 早停和标准规模断言，保持 Story 1.6/1.7/1.9 边界及单一 Analyzer/GraphPatch/GraphStore 主干。
- Validation-fix continuation attempt 10：以 VALIDATION10 artifact 建立组合负载 RED 证据；将测试中与量化语义无关的 metadata/blocked 磁盘 I/O 注入为不可变虚拟夹具，复用生产 64 KiB 流式 SHA-256 核心；随后重跑目标文件、CR5 聚焦、两个曾失败 gate 与全部指定静态/边界验证，并独立审计 57/57/57 ownership。
- Validation-fix continuation attempt 11：分别以受控 discovery 探测延迟和 VALIDATION11 后代 marker 日志建立两个 RED 根因；合同测试注入确定性 discovery 状态而生产入口继续使用真实文件系统，Windows deadline runner 同时启动独立进程组终止与 `taskkill /T /F` 树回收，并在不增加 timeout、观测次数或 cleanup grace 的前提下完成指定稳定性与 gate 验证。
- Review-fix continuation attempt 12：严格按 CR6-001→CR6-013 建立独立命名 RED，再以真实文件系统大小写探测、manifest-casing 虚拟 host、分区有界增量缓存、树级 taskkill 结算、主线程协议重算和 fatal BOM 解码完成最小 GREEN/REFACTOR；保持 Story 1.5 量化边界与后续 VALIDATION13 分工。
- Review-fix continuation attempt 13：严格按 CR7-001→CR7-007 建立独立命名 RED，再以同卷只读大小写证明、完整 case-fold 虚拟目录语义、workspace metadata fence、500000 LOC 有界 Program 复用、taskkill 128 后代证明、clean-checkout 构建拓扑和 package manifest 边界完成最小 GREEN/REFACTOR；完整 24/24 留给 VALIDATION14。
- Review-fix continuation attempt 15：严格按 CR8-001→CR8-006 建立聚焦 RED，再以 TypeScript host identity、全链路 `node_modules` 大小写传播、service-client clean build、Windows Job Object、完整 workspace package snapshot 与安全 npm subpath 语义完成最小 GREEN/REFACTOR；完整 `architecture-required` 留给下一独立 VALIDATION。
- Validation-fix continuation attempt 16：先在完整并行 unit 中稳定复现短 Git helper 的 `EPROCESSCLEANUP`，再移除每次调用的 PowerShell `Add-Type`/stdin host 启动开销，以相邻可审计 C# 源码、摘要校验的预编译程序集和 `-EncodedCommand` 保持同一 Job Object fail-closed 语义；完整 `architecture-required` 留给 VALIDATION17。
- Review-fix continuation attempt 17：以 CR9-001/002/003/005/006/007/008 的共同机制建立 7 组聚焦 RED；分别收敛根 metadata absent fence、配置目录基准、project 完整性置信度、Worker admission、specifier 分类、虚拟目录枚举和 ENOTDIR 候选语义，不触碰 CR9-004 或 Story 5.6 长时资源硬化。

### Debug Log References

- DS attempt 17 RED：4 个聚焦测试文件共 116 项，新增/强化的 10 个断言稳定失败，覆盖根 metadata absent/read-set、嵌套配置父级/兄弟 include、不完整 project 静态置信度与主线程 payload 校验、Worker 解码/AST 前 admission、含冒号 package subpath、U+FFFF 后代枚举及 ENOTDIR/权限错误分类。
- DS attempt 17 GREEN：根级 metadata 名称统一进入 absent CAS 与原生 watcher；`readDirectory` 从 include 固定前缀选择受控枚举根；不完整 project 的所有静态 resolved target 统一为 medium 且主线程拒绝伪 high；主线程在 Worker/structured clone/byte cache 前、Worker 在解码/目录索引/Program 前执行同一数量与字节 admission；external fallback 复用 npm specifier 分类；后代枚举改为 lower-bound 后逐项前缀验证；resolution candidate 仅将 `ENOENT|ENOTDIR` 归为缺失。
- DS attempt 17 验证：聚焦 4 文件 116/116；Analyzer adapter 与 graph-service package type 通过；目标 ESLint 与 test-marker 通过；官方 TypeScript module analysis verifier 12 个 unit 文件 280/280、真实进程 contract 6/6 且 8 个依赖包构建通过。未运行或宣称完整 `architecture-required`，留给下一全新 VALIDATION。
- DS attempt 17 边界：未触碰 CR9-004、`packages/application/src/ports/analyzer-port.ts`、CI/quality-gate、controller/registry/PR #9 或 Story 5.6 长时资源硬化；未提交、暂存、push、创建/更新 PR 或合并。
- DS attempt 16 RED：聚焦 `gate-applicability` 可通过但真实 SHA-1/SHA-256 Git 场景分别约 37/13 秒；完整并行 unit 稳定出现 `unit::gate-applicability::real-git::win32-process-cleanup::EPROCESSCLEANUP|45000ms-timeout+EBUSY`，新增 8 路短 Git 回归在原实现下同步失败。
- DS attempt 16 根因：每次 Windows 进程调用都启动 PowerShell 并动态 `Add-Type` 编译同一 C# Job host；在并行 Analyzer/SQLite/heartbeat 负载下 helper 建立超过既有 10 秒 cleanup grace。仅改为预编译程序集后，`pwsh -Command -` 的 stdin host 解析仍使完整 unit 中 7/8 短 Git 未及时 ready。
- DS attempt 16 GREEN：保留同一 `CREATE_SUSPENDED → AssignProcessToJobObject(KILL_ON_JOB_CLOSE) → ready marker → ResumeThread` 顺序，将可审计 C# 源码与 5632-byte 程序集共同内嵌并在加载前校验 SHA-256；helper 使用 `-EncodedCommand` 且工作目录固定在 `%SystemRoot%`，目标 cwd 通过环境变量传入，避免 helper 在启动/失败阶段持锁临时仓库。
- DS attempt 16 验证：`process-deadline` 10/10、`gate-applicability` 7/7、完整 unit 588/588、完整 contract 188/188、官方 Story verifier 272/272 unit + 6/6 contract、`pnpm type` 与 `pnpm lint` 通过；未运行或宣称完整 `architecture-required`。
- DS attempt 15 RED：聚焦回归稳定复现 `İ.ts`/`i\u0307.ts` 误合并、Unicode watcher 误命中、`NODE_MODULES` internal-file 误标与 exports unresolved 伪边、metadata broker 误走 blocked、verifier 缺少 service-client、workspace package 丢失 name、`@`/`!` subpath invalid，以及 intermediate 退出后 detached grandchild 存活并写 marker。
- DS attempt 15 GREEN：application 提供唯一 TypeScript-compatible host path identity，Worker、manifest、glob/containment、watcher、broker 与 package boundary 共用；Windows 默认执行在恢复目标前加入 `KILL_ON_JOB_CLOSE` Job Object，50ms deadline 不变且不再依赖断链 CIM；AD-3 与 npm subpath 合同同步补齐。
- DS attempt 15 验证：聚焦 unit 110/110、process-deadline 9/9、failure-propagation 32/32、quality-gates manifest 9/9、`pnpm type` 与 `pnpm lint` 通过；官方 TypeScript module analysis verifier 按含 service-client 的 clean 拓扑构建并通过 272/272 unit + 6/6 contract。未运行或宣称完整 `architecture-required`。
- DS attempt 14 RED：VALIDATION14 的真实挂起 gate 在当前负载下间歇出现 `win32:absolute-deadline:expected-ETIMEDOUT:actual-EPROCESSCLEANUP`；确定性回归进一步证明 `taskkill=128` 后旧实现会在根进程 `close` 前过早执行单次后代快照，将已清理的无后代根进程误分类为 `EPROCESSCLEANUP`。
- DS attempt 14 GREEN：Windows cleanup 在原 10 秒同一宽限内先等待根进程 `close`，再执行一次 CIM 后代级收敛证明；不增加 retry、不提高 50ms deadline/cleanup grace，`taskkill=128` 且脱离后代仍存活或无法证明时仍稳定返回 `EPROCESSCLEANUP`。
- DS attempt 14 验证：精确 RED→GREEN；failure-propagation 最终代码连续 3 轮均 32/32；process-deadline 连续 5 轮均 8/8；完整 contract 188/188、unit 577/577；type、lint、build 通过；deterministic rebuild 166/166 + 6/6；TypeScript module analysis 263/263 + 6/6；`git diff --check` 通过。未运行或宣称完整 `architecture-required`，保留给 VALIDATION15。
- DS attempt 14 审计：branch=`codex/story-1-5`、HEAD/base=`034cc3e0980dcfce6af59abe80436305fc242767`、0 staged，Story File List/controller ownership/实际有效变更为 60/60/60，无额外路径；`.gitignore` blob=`9304c5fcecaaf304ce0cdf38644e17e6a0c352e3`，禁止路径保持原状，诊断期间意外产生的空临时文件 `{}` 已精确移除，最终零临时污染。
- DS attempt 13 RED：CR7 聚焦测试分别复现无 ASCII indexing root 被误判为大小写敏感、case-insensitive project sourcePaths 为空、`pnpm-workspace.yaml` 未进入 digest/watcher、500000 LOC 状态未缓存、taskkill 128 脱离后代误报 pass、verifier 无 clean-checkout build manifest，以及 exports 拒绝 package 被伪造为 unresolved edge。
- DS attempt 13 GREEN：同卷探测优先使用既有路径组件/Windows 卷根，POSIX 在 128 层目录与每层 256 项内只读寻找同实体大小写变体；VirtualFileMap/VirtualDirectoryIndex/glob/extension/project membership 共用 host key 并返回 manifest casing；`pnpm-workspace.yaml` 进入根 metadata、digest、watcher 与 COMMIT fence且 `workspacePackages=[]`；Program 缓存只计实际持久对象，以 40 MiB project + 8 MiB syntax、48 MiB 总上界复用 50 project/5000 file/500000 LOC；taskkill 128 在同一 grace 内以单次 CIM 快照递归证明后代收敛；verifier 固定执行 domain→contracts→application→三 adapter→graph-service；manifest 已存在但 exports/subpath 失败只诊断不建伪边。
- DS attempt 13 验证：7 项聚焦 10/10；承载文件 unit 84/84、contract 9/9；Analyzer 连续 3 轮均 43/43；最终 process-deadline 连续 5 轮均 8/8；stale-token 连续 5 轮且 service-client 25/25；官方 verifier 263/263 + 真实进程 6/6；完整 unit 577/577、contract 188/188；type（初次发现测试注入签名后最小修正并复验）、lint、build、dependency-boundary、basic-security、planning-trace、repository preflight、verifier manifest 9/9、failure-propagation 32/32、deterministic rebuild 166/166 + 6/6、`git diff --check` 全部通过。未运行或宣称完整 24/24 `architecture-required`。
- DS attempt 13 审计：branch=`codex/story-1-5`、HEAD=`034cc3e0980dcfce6af59abe80436305fc242767`、0 staged、Story File List/controller/实际有效变更 60/60/60，集合完全一致；无新增 Story 路径，`.gitignore` blob=`9304c5fcecaaf304ce0cdf38644e17e6a0c352e3`，既有 unowned/禁止路径状态保持不变且未产生测试临时污染。
- DS attempt 12 RED：13 个 `CR6-xxx` 聚焦测试逐项失败，分别复现大小写 import 漏边、带点目录 exclude 穿透、主线程 metadata casing 漏分类、50/5000 Program 零复用、根 exit 隐藏 taskkill 失败、MAX+1 前 source membership 已执行、越界 UTF-16 range、静态 confidence 降级穿透、npm `+` 子路径拒绝、模板 literal 误诊断、两个 timer 溢出和 UTF-16 BOM fatal UTF-8。
- DS attempt 12 GREEN（CR6-001..006）：graph-service 对 indexing root 做只读卷语义探测并传入 Worker；虚拟文件表以 host case-fold 查找且 `realpath` 固定回 manifest casing；exclude 的祖先目录语义不再猜测点号；主线程与 watcher Worker 共用 NFC case-fold key；Program/语法缓存分别受 24 MiB/8 MiB 分区和 32 MiB 总上界约束，50 project/5000 file 第二轮保持 `programBuilds=50` 且 `programReuses>=50`；Windows 只以已启动的 `taskkill /T /F` 结果结算；精确 MAX Program 可通过，MAX+1 在 source membership 前拒绝。
- DS attempt 12 GREEN（CR6-007..013）：主线程以相同 fatal BOM 合同计算每个源码 UTF-16 长度并封闭校验 diagnostic/binding/relation；由 dynamic、unresolved external、internal project 完整性和规范 target 重算 confidence；npm subpath 仅增加合法 `+`；`import()`/`require()` 使用 `StringLiteralLike`；所有 Node timer 参数限制为 `2_147_483_647`；Worker 兼容 UTF-16LE/BE BOM 且损坏编码继续 fatal，不输出源码正文或绝对路径。
- DS attempt 12 稳定性与门禁：最终代码上 `analyzer-config-capture.test.ts` 连续 3 轮均 36/36，`process-deadline.test.ts` 连续 5 轮均 7/7；stale-token 目标连续 5 轮、完整 service-client-control 25/25；相关 4 文件 69/69；官方 Story verifier 255/255 + 真实进程 6/6；完整 unit 568/568、contract 187/187；type、lint/marker、独立 marker、dependency-boundary、basic-security、planning-trace、repository preflight、verifier manifest 8/8、failure-propagation 32/32 全部通过。50ms gate deadline、600ms marker、500ms/5ms/三次 stale、默认 Windows 10 秒 cleanup grace、50 project/5000 file 与 MAX/MAX+1 均未放宽；完整 24/24 `architecture-required` 明确保留给 VALIDATION13。
- VALIDATION attempt 11 contract RED：保持 500ms/5ms/三次 stale 合同，单次 discovery 探测人为延迟 170ms 时仅完成两次 stale 观察并约 505ms 误报 `SERVICE_START_TIMEOUT`；延迟 20ms 时约 138ms 完成三次 stale 并正确返回 `SERVICE_INSTANCE_CONFLICT`，确认连续观测逻辑正确，根因是合同测试依赖真实 `lstat` 抖动。
- DS attempt 11 contract GREEN：`connectToGraphServiceWithCacheRootForTests` 增加未从包根导出的 test-only discovery 注入；生产 `connectToGraphService` 仍固定使用真实发现。launcher 继续只创建真实 32-byte token，测试明确断言 `absent → stale → stale → stale`；目标测试连续 5 轮通过，完整 `service-client-control.test.ts` 25/25、完整 `pnpm contract` 187/187 通过。
- VALIDATION attempt 11 unit RED/GREEN：原始日志证明 50ms deadline 返回 `ETIMEDOUT` 后，后代仍在 600ms 观察窗内写 marker；根因是仅异步启动的 `taskkill` 在高负载冷启动下晚于后代动作。Windows 现以 detached 独立进程组启动并立即广播 `SIGBREAK`，并行执行 `taskkill /T /F`、等待有界收敛；高负载 exit 事件延迟时只读核验根 PID，两个机制均失败才返回 `EPROCESSCLEANUP`。默认 Windows cleanup grace 保持 10,000ms，POSIX 路径未改。
- DS attempt 11 稳定性与 gate 证据：最终代码上的 `process-deadline.test.ts` 连续 5 轮均 5/5，完整 `pnpm unit` 557/557；CR5 聚焦 8 文件 204/204，官方 Story verifier 12 个 unit 文件 246/246、真实进程 6/6；`pnpm type`、lint/marker、独立 marker、dependency-boundary、basic-security、planning-trace、repository preflight、verifier manifest 8/8、failure-propagation 32/32 与 `git diff --check` 全部通过。按 DS 边界未运行完整 24/24 `architecture-required` 或后续 VALIDATION/CR/QA/SS/CS/VS/ER。
- VALIDATION attempt 10 RED：独立完整目标文件虽 33/33 通过，但耗时 59.39 秒；blocked 预算、128 层与主进程 MAX+1 分别为 21.374 秒、4.171 秒、9.156 秒，结合 architecture-required artifact 中同一文件的 1+3 个测试级 timeout，确认根因是组合负载下真实磁盘往返与大字节物化造成的时限抖动，而非断言失败。
- DS attempt 10 GREEN：新增可注入的 Analyzer 配置捕获 I/O 边界；生产默认路径继续执行 no-follow、realpath containment、文件身份前后复核和真实流式 hash，测试仅虚拟化与语义无关的 metadata/blocked 磁盘操作。9 MiB、1025 文件、128/129 层、50 project/5000 file、4096+1/MAX+1 与 64 KiB 峰值缓冲断言均未缩小。
- DS attempt 10 稳定性与 gate 证据：目标文件首轮降至 13.82 秒，连续三轮 33/33（22.36、24.94、32.50 秒，测试执行最慢 15.16 秒）；CR5 聚焦 120/120、官方 Story verifier 246/246 与真实进程 6/6、完整 `pnpm unit` 557/557、单独 typescript-module-analysis-v1 gate 246/246 与真实进程 6/6 均通过。
- DS attempt 10 静态与边界验证：`pnpm type`、完整 lint+marker、dependency-boundary、basic-security、planning-trace、repository preflight、failure-propagation 32/32 与 `git diff --check` 通过；按 DS 边界未运行完整 architecture-required 或后续 VALIDATION/CR/QA/SS/CS/VS/ER。
- DS attempt 10 污染恢复：仅移除 VALIDATION10 追加的 `.tmp-*/`，根 `.gitignore` 与 integration base 长度 166、SHA-256 `c97ab71e1d40080a9b7e0f4d02ff39b387b90a8ba471f11f96f5b04ee52fde9b`、Git blob `9304c5fcecaaf304ce0cdf38644e17e6a0c352e3` 逐字节一致；未读取或修改 unowned 内容。
- Review attempt 5 RED：分批稳定复现 7 个边界/语法/verifier 失败、6 个 Worker/预算/复杂度失败和 6 个 SQLite 失败；既有对应回归分别保持 76、35 与 16 项通过，覆盖 CR5-001 至 CR5-018 的独立失败入口。
- Review attempt 5 GREEN：Analyzer blocked capture 使用文件数/总字节 stat 预扣与固定 64 KiB 流式 SHA-256；COMMIT fence 在事务外准备完整字节证明，事务内只复核 dev/ino/size/mtime/ctime 与不存在性；Worker host 重算 source/language/confidence/internal/external 规范身份，所有伪造成功 payload 收敛为 `ANALYZER_PROTOCOL_INVALID`。
- Review attempt 5 量化证据：50 project/5000 file 标准规模通过，SourceFile 在唯一对象 `MAX+1` 早停；配置目录访问不超过 3F；facts/clone sink 在插入前执行 MAX+1，未对超预算 payload 使用 `JSON.stringify`；blocked 9 MiB+9 MiB 在第二个文件读取前拒绝，1025 文件在第 1025 个读取前拒绝，峰值缓冲 64 KiB。
- Review attempt 5 GREEN：SQLite tokenizer 只提取 comment/quoted context 外独立 CHECK token；`index_xinfo` 锁定 BINARY/ASC/key 列；Evidence 提交前与恢复均要求唯一 source owner；hierarchy/Evidence/shared node 的 canonical payload 篡改均 fail-closed；Story 1.5 保留孤立共享节点并把回收继续留给 Story 1.7。
- Review attempt 5 验证：官方 Story verifier 12 个 unit 文件 246/246、真实 graph-service process 6/6、verifier manifest contract 8/8 通过；`pnpm type`、完整 ESLint+marker、dependency-boundary 与 `git diff --check` 通过。按 DS 边界未运行完整 `architecture-required` 或后续 VALIDATION/CR/QA/SS/CS/VS/ER。
- Review attempt 4 RED：CR4 聚焦集先稳定复现 9 个失败测试，覆盖 blocked/absent resolution、128 层稳定确认、host target、metadata ACK close、package/source 索引复杂度、type-only require、facts 与 Program/AST 对象预算；composite hierarchy-only 回退、MAX+1 读取、typed Worker 分类、quote-aware CHECK 与过期八轮文档分别以新增合同/篡改/边界断言封口后转绿。
- Review attempt 4 GREEN：配置快照新增互斥的 `blockedResolutionFiles` 与 `absentResolutionFiles`；blocked 源码只以空占位交由 TypeScript 权威 resolver 诊断，不向 Worker 交付正文、不建 npm edge/Evidence，并与 consulted/absent 一并进入 watcher、同步提交前后 fence 与 SQLite read-set 恢复校验。
- Review attempt 4 量化证据：package fallback 对虚拟文件只做单遍索引，relation 查询按计数保持 O(1)；source/project membership 单遍建立 Map 并在 observe/analyze O(1) 复用；metadata 增长最多读取 `MAX+1` 字节；Worker 分别限制每文件/每请求 facts、估算 clone bytes、Program、SourceFile、目录条目与保守 retained bytes。
- Review attempt 4 GREEN：composite builder 在入口强制完整 committed graph snapshot；Worker/host/runtime 双层校验 builtin 与 internal target；metadata 请求 close/abort/timeout/postMessage 竞态全部结算；SQLite CHECK 比较仅规范引号外文本；type-only import binding 不再遮蔽运行时 `require`。
- Review attempt 4 验证：5 个相关 package build 通过；CR4 聚焦 98/98、官方 Story verifier 119/119、QA runtime 28/28、真实进程 6/6 通过；`pnpm type`、目标 ESLint、marker、dependency-boundary、`git diff --check` 与 File List/controller ownership 57/57 均通过。按 DS 边界未运行完整 `architecture-required` 或后续 VALIDATION/CR/QA/SS/CS/VS/ER。
- DS production defect attempt 7 先由真实进程聚焦 contract 稳定复现 `SERVICE_PROTOCOL_INCOMPATIBLE`，再在 `index-job-runtime` 增加 `validateServiceStatusV1` RED，精确定位为全图模块 edge 计数被直接映射到公共 committed 摘要，违反 hierarchy 树 `edgeCount = nodeCount - 1` 语义不变量；失败 Job 的 `GRAPH_SCAN_FAILED`、stale、base/result revision 与时间序列本身均合法。
- DS attempt 7 将 SQLite 全图持久摘要与 ServiceStatusV1 hierarchy 摘要分离：提交后用本轮 hierarchy batch 生成公共计数，重启恢复时用持久 snapshot 的 hierarchy ownership slice 重建公共计数；未修改 validator、server 响应校验、协议 schema 或错误分类。应用层 28/28、真实进程 6/6、官方 Story verifier 108/108、`pnpm type`、目标 ESLint、marker 与 diff-check 均通过。
- VALIDATION attempt 6 的 `TS2345` 已由 `pnpm type` 精确复现；对 `second.moduleResolutionCacheHits` 增加显式 `undefined` 失败分支后，`pnpm type`、聚焦 `analyzer-config-capture.test.ts` 21/21 与官方 Story verifier 107/107 均通过。
- Review attempt 3 RED：领域/目标解析/语法/时间边界与 SQLite 两轮聚焦执行分别复现 6 个和 5 个失败断言，共 11 个确定性 RED；配置闭包、候选预算、watcher COMMIT 窗口与深层 extends 场景也均先以独立失败用例确认后再修复。
- Review attempt 3 GREEN：5 个项目引用 TypeScript 构建通过；9 个核心聚焦文件 127/127 通过，3 个相邻配置快照/复合 patch/SQLite 回归文件 87/87 通过；Story File List 中全部 TS/MJS 的 ESLint、`check-test-markers`、`git diff --check` 与 `pnpm dependency-boundary` 均通过。
- Review attempt 3 量化证据：持久 Worker 断言跨请求 Program/SourceFile/模块解析缓存复用，并限制项目数、缓存条目、UTF-8 字节与语法事实；SQLite 以 `EXPLAIN QUERY PLAN` 证明 Evidence 查询使用 `evidence_workspace_edge_source_idx`，10,000 行规模回归保持有界。
- Review attempt 3 按 DS 约束未执行完整 blocking gate，也未进入 VALIDATION/CR/QA/SS/CS/VS/ER；新的 VALIDATION 工作流应在 WORKTREE 上执行全量门禁。
- VALIDATION attempt 3 的 `type` 失败已在改动前由 `pnpm type` 精确复现：`tests/unit/typescript-module-syntax.test.ts:153:45` 把宽化后的 `ts.ModuleKind` 传给要求 `ts.ResolutionMode` 的公共合同；测试夹具显式绑定 `ExtractModuleSyntaxFactsOptions` 后，`pnpm type` 与聚焦语法测试 8/8 通过。
- Review attempt 2 RED：8 个聚焦文件中复现 17 个失败断言、139 个既有断言通过；覆盖全部 16 项 finding，其中 manifest/ownership 性能分别以属性访问次数、Worker 复制以跨请求字节数量量化。
- Review attempt 2 GREEN：同一 8 个聚焦文件 157/157 通过；`@codegraph/domain`、`@codegraph/adapter-analyzer-typescript`、`@codegraph/adapter-store-sqlite`、`@codegraph/graph-service` 类型检查通过；目标文件 ESLint 与 `check-test-markers` 通过。
- Review attempt 2 按 DS 约束未执行完整 blocking gate；新的 VALIDATION 工作流应在 WORKTREE 上执行全量门禁。
- Review attempt 1 RED：聚焦集先复现 14 个失败，覆盖配置入口/extends/references、路径与 broker、语法/解析、Worker payload 与关闭幂等。
- `pnpm type`、`pnpm lint`、`pnpm build`、`pnpm dependency-boundary`、`pnpm basic-security`、`pnpm planning-trace`：通过。
- `pnpm unit`：63 个测试文件，484/484 通过。
- `pnpm contract`：21 个测试文件，185/185 通过。
- `node scripts/ci/verify-typescript-module-analysis-v1.mjs`：10 个测试文件，70/70 通过。
- `node scripts/ci/verify-deterministic-rebuild-v1.mjs`：7 个 unit 文件 155/155、1 个 contract 文件 5/5 通过。
- `pnpm architecture-required` 的本次组合重跑被调用方 184 秒上限终止，未返回 gate 失败；其本次涉及的 type/lint/unit/build/contract/dependency/security/planning、Story 1.5 与 deterministic 专项均已分别通过，后续独立 revalidation 可重跑完整组合器。
- 固定 baseline `86bd1338d333cedbb0aab19508c786915175eab7` 对合成 head `e82d0afd4ba59db471284b14d7125a2b0d7973ed`：`PUBLIC_CAPABILITY_VIOLATIONS=[]`。
- `git diff --check`：通过，仅有工作区 CRLF 转换提示。

### QA Automation Record

- QA follow-up `QA1-GRAPH-SERVICE-INVALID-FAILURE-STATUS` 已由 DS attempt 7 解决：真实进程在根外 `baseUrl` 失败后保持连接可用，返回 `GRAPH_SCAN_FAILED`、`freshness=stale`、committed/graph/result revision 1，并可继续 `service/status` 与 `service/shutdown`；SQLite revision、module edge 与 Evidence 保持原子不变。QA 夹具仅把公共 hierarchy `edgeCount` 从误写的全图 4 校正为 3，原有模块 edge/Evidence SQLite 断言全部保留，未削弱风险合同。
- QA generate high-risk integration/E2E coverage attempt 1（2026-07-28）新增两条已归属 blocking 回归，不新增依赖或路径：
  - `tests/unit/index-job-runtime.test.ts` 使用真实 TypeScript Worker、`extends + paths`、ModuleSourceFactBatch、CompositeGraphPatch 与真实 SQLite transaction；在 consulted `configs/base.json` 精确落入 COMMIT 窗口后证明首次 composite mutation 全回滚，同一 logical Job 按新 paths 重排并只提交 `graphRevision=1`。该文件 28/28、官方 Story verifier 108/108 通过。
  - `tests/contract/graph-service-process.test.ts` 通过真实 graph-service 进程和公共协议把 `extends + paths` 模块事实提交到 SQLite，再把配置改为根外 `baseUrl` 验证失败分类、stale 与旧 revision 原子保留。该回归当前暴露生产缺陷：失败后 `service/status` 不满足 `ServiceStatusV1`，服务返回 `SERVICE_PROTOCOL_INCOMPATIBLE`，无法观察预期的 `GRAPH_SCAN_FAILED + stale + graphRevision=1`；合同文件当前 5/6，通过修复前必须保持阻断。
- 逐风险审计结论：SQLite absent/v1/v2/v3、并发 opener、恢复幂等、exact DDL/index、Evidence workspace/edge/source 拓扑与跨 workspace 拒绝、Program/解析缓存数量和字节上界、候选数量/路径字节预算、Evidence `EXPLAIN QUERY PLAN` 与 10,000 行非二次规模证据均已有真实 blocking 自动化，不重复增加单元断言。
- QA 边界：未修改生产代码，Story/Sprint 保持 `review`；本轮未运行完整 `architecture-required`，新增内容须由修复后的新 VALIDATION 从头复验。

### Completion Notes List

- 完成 DS fix CR9 findings attempt 17：CR9-001/002/003/005/006/007/008 已按共同机制完成 RED→GREEN→REFACTOR，Story/Sprint 恢复 `review`；下一步仅交回全新独立 VALIDATION，未启动 CR/QA/DIAGNOSIS/PLATFORM-FIX/SS/CS/VS/ER。
- ✅ Resolved CR9-001：7 个根级 Analyzer metadata 名称无论存在或缺失都进入同一快照/read-set 与 watcher，创建、删除或 rename 会使当前证明失效。
- ✅ Resolved CR9-002：嵌套 tsconfig 的 include 以配置目录为基准，并从固定前缀枚举父级/兄弟源码；虚拟工作区 containment、ignore、extension、depth 与既有目录预算保持不变。
- ✅ Resolved CR9-003：不完整 project context 的 builtin、resolved external 与 internal 静态边统一降为 medium；主线程按冻结 project 完整性重算并拒绝伪造 high payload，既有配置缺失诊断/失败语义不放宽。
- ✅ Resolved CR9-005：主线程在 Worker 创建、structured clone 与 byte cache 更新前，Worker 在完整 decode、目录索引和 AST/Program 构建前，按 5000 source、6144 total、64 MiB raw 与 1 MiB path 执行确定性 admission，超限稳定为 `ANALYZER_RESOURCE_LIMIT`；未实现 Story 5.6 长时 RSS/CPU/8h/流式 Program 项。
- ✅ Resolved CR9-006/007/008：合法含冒号 package subpath 复用唯一 npm 分类并保持 exports 拒绝诊断、不伪造 unresolved；U+FFFF 文件名不再被 prefix upper-bound 遗漏；仅 `ENOENT|ENOTDIR` 作为 resolution candidate 缺失，`EACCES` 等真实 I/O 错误继续 fail-closed。
- 验证结果：聚焦 116/116、官方 Story verifier 280/280 unit + 6/6 contract、两个相关 package type、目标 ESLint 与 marker 全部通过；完整 `architecture-required` 明确保留给下一独立 VALIDATION。
- ownership 审计：本轮 11 个 tracked changed paths 均已存在于 Story File List，0 staged；受保护的 6 个 unowned untracked 路径保持未读取、未修改、未暂存、未删除。
- 完成 DS validation-fix attempt 16：消除完整并行 unit 下短生命周期 Git 的 Job helper 启动竞争，Story/Sprint 保持 `review`；未启动 VALIDATION17/CR/QA/SS/CS/VS/ER，未提交、暂存、推送、创建 PR 或合并。
- ✅ Resolved VALIDATION16：Windows 默认执行路径不再为每个短进程动态编译 C#；预编译程序集加载前固定摘要校验，Job Object 仍在目标恢复前绑定并以最后句柄关闭作为 detached-grandchild 内核收敛点，任何 bootstrap、cleanup 或证明失败继续稳定返回 `EPROCESSCLEANUP`。
- 回归证明：8 路并行短 Git 全部正常退出且临时仓库由测试正常 teardown；detached grandchild 被可靠终止且不写 marker；timeout 保持 `ETIMEDOUT`，cleanup 失败保持 fail-closed。最终完整 unit 63/63 文件、588/588 测试，contract 21/21 文件、188/188 测试，官方 Story verifier、type、lint 均通过。
- 污染边界：控制器补充禁删指令到达前曾手工删除首次 RED 产生的 `codegraph-gate-git-EnaNX4` 与 `codegraph-gate-git-dbMOu6`；其后未删除任何 `codegraph-gate-git-*`。最终集合保持任务接续基线的 16 个既有目录（含 VALIDATION16 的 `LrOMLC`、`Nbtagg`、`rtOfQl`），本轮最终修复/验证新增 0；5 个受保护 heartbeat Node PID 与既有 Git PID 74368 均未终止。
- 最终审计：branch=`codex/story-1-5`、HEAD/base=`034cc3e0980dcfce6af59abe80436305fc242767`、Story File List/controller ownership/实际有效变更集合精确 60/60/60，0 staged，`.gitignore` blob=`9304c5fcecaaf304ce0cdf38644e17e6a0c352e3`；普通 `git diff --check` 与 planning trace 通过，无新增 Job helper、Git 进程或本轮测试临时目录。
- 完成 DS fix CR8 findings attempt 15：CR8-001..CR8-006 全部按 RED→GREEN→REFACTOR 修复，Story/Sprint 恢复 `review`；未启动后续 VALIDATION/CR/QA/SS/CS/VS/ER，未提交、推送、创建 PR 或合并。
- ✅ 统一宿主路径 identity 并贯穿虚拟文件/目录、glob、containment、manifest、read-set/watcher、metadata broker 与全部 package boundary；大小写不敏感 host 的非 ASCII 等价与反例均有回归，case-sensitive 行为保持封闭。
- ✅ clean verifier 显式构建 service-client；Windows Job Object 在祖先断链后仍由内核收敛全部后代；workspace package snapshot 保留 root/name；合法 `@`/`!` npm subpath 产生 resolved 或 medium unresolved，路径/URL/编码逃逸继续拒绝。
- 验证结果：聚焦 unit 110/110、process-deadline 9/9、failure-propagation 32/32、quality manifest 9/9、type、lint、官方 Story verifier 272/272 + 6/6；File List 复核仍为 60 路径且生产 `workspacePackages=[]`。完整 `architecture-required` 明确留给下一独立 VALIDATION。
- 完成 DS validation-fix attempt 14：仅修复 VALIDATION14 的 Windows absolute-deadline 分类失败，Story/Sprint 恢复 `review`；未运行后续 VALIDATION15/CR/QA/SS/CS/VS/ER，未提交、推送、创建 PR 或合并。
- ✅ Resolved VALIDATION14：`taskkill=128` 后必须在同一 cleanup grace 内先取得根进程 `close` 收敛，再执行单次后代级证明；无后代根进程可稳定保留 `ETIMEDOUT`，脱离后代仍存活、CIM 确认未收敛或无法证明时仍返回 `EPROCESSCLEANUP`。
- 验证结果：RED→GREEN；3×32/32 failure-propagation；5×8/8 process-deadline；contract 188/188；unit 577/577；type/lint/build；deterministic rebuild 166/166 + 6/6；TypeScript module analysis 263/263 + 6/6；`git diff --check` 全部通过。完整 `architecture-required` 明确留给 VALIDATION15。
- ownership 审计：Story File List/controller ownership/实际有效变更精确 60/60/60，0 staged、无额外路径、`.gitignore` blob 不变，禁止路径未读取或修改，最终零临时污染。
- 完成 DS fix CR7 findings attempt 13：CR7-001..CR7-007 全部按独立 RED→GREEN→REFACTOR 处理，Story/Sprint 恢复 `review`；未运行后续 VALIDATION14/CR8/QA/SS/CS/VS/ER、未提交/推送/创建 PR。
- ✅ Resolved CR7-001：全数字/全非 ASCII indexing root 通过 Windows 卷根或 POSIX 同卷既有实体只读证明识别大小写不敏感语义，无法证明时继续 fail-closed。
- ✅ Resolved CR7-002：虚拟目录 key、枚举、extensions、include/exclude glob、project membership 和内部依赖解析完整遵循 `caseSensitiveFileNames=false`，输出保持 manifest casing。
- ✅ Resolved CR7-003：`pnpm-workspace.yaml` 纳入 configurationFiles/consultedFiles/configDigest、watcher semantic event 与 COMMIT fence；Story 1.9 前 `workspacePackages` 仍为空。
- ✅ Resolved CR7-004：移除未持久原始字节的重复计量，以实际目录索引、Program/SourceFile 与解析状态计入 40 MiB project 分区；50/5000/500000 LOC 两请求复用 50 个 Program，总缓存仍受 48 MiB 上界约束。
- ✅ Resolved CR7-005：taskkill 128 不再等价于树收敛；同一 cleanup grace 内必须取得递归后代快照无存活证明，否则稳定返回 `EPROCESSCLEANUP`，无 retry/额外 timeout。
- ✅ Resolved CR7-006：官方 Story verifier 固定 clean-checkout 拓扑并补齐 domain、contracts、git-local 前置构建，不依赖预留 dist。
- ✅ Resolved CR7-007：package 根不存在仍可生成 medium unresolved purl；manifest 已存在但 exports/subpath 解析失败稳定诊断 `MODULE_EXTERNAL_PACKAGE_METADATA_INVALID` 且不生成伪边。
- 验证结果：聚焦 10/10、相关 93/93、3×43 Analyzer、最终 5×8 process、5×stale-token、service-client 25/25、官方 verifier 263/263 + 6/6、unit 577/577、contract 188/188、deterministic rebuild 166/166 + 6/6 与全部指定静态/边界检查通过；完整 24/24 留给 VALIDATION14。
- ownership 审计：Story File List/controller/实际有效变更继续精确 60/60/60、0 staged、无额外 Story 路径、`.gitignore` blob 不变；既有 unowned/禁止路径未读取或修改，无临时污染。
- 完成 DS fix CR6 findings attempt 12：CR6-001..CR6-013 全部按独立 RED→GREEN→REFACTOR 处理，Story/Sprint 恢复 `review`；未运行后续 VALIDATION/CR/QA/SS/CS/VS/ER，完整 24/24 `architecture-required` 留给 VALIDATION13。
- ✅ Resolved CR6-001：真实卷大小写语义贯穿 graph-service→Worker，大小写不敏感查找后解析路径回到 `src/dep.ts` 的 manifest casing；大小写敏感卷仍保持严格。
- ✅ Resolved CR6-002：wildcard exclude 对命中的目录祖先统一排除后代，`**/.generated` 与 `**/generated.v2` 不再进入 project。
- ✅ Resolved CR6-003：主线程 consulted/absent/blocked metadata 与 watcher Worker 使用同一 case-fold key，大小写变化立即失效当前 proof。
- ✅ Resolved CR6-004：Program 与语法事实采用 24 MiB + 8 MiB 分区、32 MiB 总上界；标准规模跨请求缓存保留且量化复用，不使用无界缓存。
- ✅ Resolved CR6-005：Windows 先在根进程存活时启动 `taskkill /T /F`，再广播进程组信号，并只等待树级结果；taskkill reject 不再被根 exit/close 隐藏。
- ✅ Resolved CR6-006：精确 MAX 项目 Program 保持可用，MAX+1 在配置解析边界立即停止且 `sourceProjectMembershipVisits=0`。
- ✅ Resolved CR6-007：Worker 三类范围均绑定所属源码 UTF-16 长度，越界成功 payload 收敛为 `ANALYZER_PROTOCOL_INVALID`。
- ✅ Resolved CR6-008：静态 builtin/resolved npm 必须为 high，unresolved external 为 medium，dynamic 为 low，不完整项目 internal 为 medium。
- ✅ Resolved CR6-009：合法 `pkg/feature+debug` 生成 medium `pkg:npm/pkg@unresolved`，package name 安全字符合同未放宽。
- ✅ Resolved CR6-010：无替换模板 literal 的 `import()`/`require()` 生成确定性精确关系，插值模板继续诊断。
- ✅ Resolved CR6-011：进程 deadline 与 cleanup grace 超过 Node timer 上限时构造前拒绝。
- ✅ Resolved CR6-012：Analyzer request timeout 超过 Node timer 上限时构造前拒绝，不再触发约 1ms 销毁。
- ✅ Resolved CR6-013：UTF-16LE/BE BOM TS/JS 源码按 fatal decoder 兼容；任意损坏编码仍拒绝且错误不泄漏正文/绝对路径。
- 验证结果：13 项聚焦 GREEN、3×36 Analyzer、5×7 process、5×stale-token、service-client 25/25、相关 69/69、官方 verifier 255/255 + 6/6、unit 568/568、contract 187/187 与全部指定静态/边界检查通过；未自称 24/24。
- ownership 审计：在既有 59 个 Story-owned 路径上仅新增获授权 review-record `tests/unit/process-deadline.test.ts`，Story File List/controller 起始 ownership+授权路径/实际有效变更为 60/60/60；无 staged，受保护未归属目录/文件未读取或修改。
- 完成 CR code review attempt 6：对 integration base `034cc3e0980dcfce6af59abe80436305fc242767` 至完整 WORKTREE 的 59 个 Story-owned 路径执行三层对抗性审查与主审复核，确认 13 项 finding（7 high、6 medium）；已按 “Leave as action items” 写入 Attempt 6，未修改生产代码、测试、配置或文档正文，Story/Sprint 保守转为 `in-progress`。
- CR6 最小复现确认：Windows 实体文件可解析的大小写差异 import 在虚拟 host 中漏边；带点目录 exclude 仍被分配到 project；50/5000 连续两轮 Program 零复用；伪 Worker 可穿透越界 range 与错误 confidence；合法 npm `+` 子路径、无替换模板 literal、超范围 timer 与 UTF-16 BOM 均稳定触发对应缺陷。VALIDATION12 的 24/24 结构化通过证据保持有效，但不替代本轮实现正确性 finding。
- 完成 DS validation-fix attempt 11：修复 VALIDATION11 的 stale-token 合同分类抖动与 Windows deadline 后代进程树泄漏；未增加合同/gate/global timeout、未减少三次 stale 观察、未放宽 50ms/600ms/marker 断言，Story/Sprint 保持 `review`。
- ✅ Resolved VAL11-001：通过 test-only discovery 边界确定性记录 `absent → stale → stale → stale`，消除与安全分类无关的真实文件系统延迟；生产默认发现、500ms 总 deadline、5ms polling、launcher 只创建 token 与 `SERVICE_INSTANCE_CONFLICT` 合同保持不变。
- ✅ Resolved VAL11-002：Windows runner 在 deadline 与正常根进程退出后均覆盖独立进程组广播和 `taskkill /T /F` 树回收，等待可验证的有界收敛并显式传播清理失败；默认 10 秒 cleanup grace 与 POSIX 行为保持不变。
- 验证结果：stale-token 目标与 Windows process-deadline 文件均连续 5 轮通过，完整合同 187/187、完整单元 557/557；CR5 聚焦 204/204、官方 verifier 246/246 + 真实进程 6/6，以及 type、lint/marker、边界、安全、规划、preflight、manifest、failure-propagation、diff-check 全部通过。完整 24/24 architecture-required 留给新的 VALIDATION attempt 12。
- ownership 审计：在原 57 个 Story-owned 路径上仅新增 `packages/service-client/src/connection.ts` 与 `tests/contract/service-client-control.test.ts` 两条 validation-record 授权路径，Story File List/controller ownership 证据/实际有效变更应为 59/59/59；未读取或修改 `.playwright-cli/`、三个 `.tmp-story-1-4-evidence-*` 目录与 `想法.md`。
- 完成 DS validation-fix attempt 10：修复 VALIDATION10 的 Analyzer 配置捕获组合负载 timeout 与 `.gitignore` 验证污染；不增加 timeout、不缩小任何量化负载，不新增 Story 路径，Story/Sprint 恢复 `review`。
- ✅ Resolved VAL10-001：通过可注入 I/O 边界让测试虚拟化 metadata 与 blocked 大字节磁盘往返，同时复用生产 64 KiB 流式 SHA-256 核心；生产默认路径的 containment、身份复核、预算预扣、MAX+1 早停与错误分类保持不变。
- ✅ Resolved VAL10-002：根 `.gitignore` 已恢复为 integration base 的 166 字节原始内容且无 diff；未读取、删除、移动或修改被隐藏的 unowned 临时目录。
- 验证结果：目标文件连续三轮 33/33，CR5 聚焦 120/120，官方 Story verifier 246/246 + 真实进程 6/6，完整 unit 557/557，单独 typescript-module-analysis-v1 gate 通过；type、lint/marker、dependency-boundary、安全、规划、preflight、failure-propagation 与 diff-check 全部通过。完整 24/24 architecture-required 留给新的 VALIDATION attempt 11。
- 完成 DS fix CR5 findings attempt 9：18 项 finding 全部按 RED→GREEN→REFACTOR 处理，Story/Sprint 恢复 `review`，未新增路径且 File List/controller ownership 保持 57/57。
- ✅ Resolved CR5-001/002：consulted/blocked 分别使用 2 MiB/10 MiB 上限；blocked 文件数/总字节在读取前预扣，流式 hash 固定 64 KiB；事务内不再读取 Analyzer 正文。
- ✅ Resolved CR5-003：Worker 文件、诊断、binding、relation、confidence 与 target 跨字段绑定；internal ID 由 resolved path 重算，external/builtin 由领域构造器重算。
- ✅ Resolved CR5-004/005：预算覆盖 50 project/5000 file，group 与 SourceFile 均 MAX+1 早停；统一 bounded sink 增量限制 facts、targets 与 clone bytes。
- ✅ Resolved CR5-006/010：SQLite CHECK 使用最小 tokenizer/parser；精确索引改用 `index_xinfo` 并拒绝 NOCASE/DESC/key 语义漂移。
- ✅ Resolved CR5-007/011：manifest-less external fallback 按 containing file 的 ancestor `node_modules` 顺序查找；配置目录使用一次排序索引和二分前缀访问。
- ✅ Resolved CR5-008/009：GraphStorePort 只返回完整 composite snapshot；Evidence 在提交入口和恢复中严格要求唯一 source ownership。
- ✅ Resolved CR5-012/013/014：四类配置路径统一排除 `rules.yaml`；default export 解包透明表达式；ambient `require` 不再误遮蔽运行时绑定。
- ✅ Resolved CR5-015/016：Story 1.5 保留孤立 external/builtin 节点；SQLite 恢复校验 hierarchy/Evidence/shared node 的唯一 canonical payload。
- ✅ Resolved CR5-017/018：大小写不敏感 watcher 使用统一 case-fold key；官方 verifier 固定包含 read-set/SQLite/真实进程并由 manifest contract 防缩小。
- 完成 DS fix review findings attempt 8：CR attempt 4 的 15 项 finding 全部按 RED→GREEN→REFACTOR 处理，Story/Sprint 恢复 `review`，未新增路径且 File List/controller ownership 保持 57/57。
- ✅ Resolved review finding [high] CR4-001：manifest 外真实源码候选安全封口为 blocked，TypeScript resolver 只产生 `MODULE_RESOLUTION_FAILED`，不生成 npm edge/Evidence。
- ✅ Resolved review finding [high] CR4-002：普通缺失 resolution candidates 独立进入 `absentResolutionFiles`、watcher 与 COMMIT 前后不存在性校验。
- ✅ Resolved review finding [high] CR4-003：composite rebuild 强制完整 allNodes/allEdges/allEvidence/ownedSlices，hierarchy-only snapshot 立即失败。
- ✅ Resolved review finding [high] CR4-004：Worker host 与 runtime 校验规范 Node builtin 及本轮 manifest internal target，非法成功 payload 归类 `ANALYZER_PROTOCOL_INVALID`。
- ✅ Resolved review finding [high] CR4-005：metadata watcher close/abort/timeout/postMessage 竞态同步清理 pending、监听器与计时器。
- ✅ Resolved review finding [high] CR4-006：请求级 package root 首候选索引替代 relation×files filter/sort，并加入访问次数上界。
- ✅ Resolved review finding [high] CR4-007：source→project/options/configurationComplete 使用冻结 Map 单遍构建、O(1) 查询并加入规模断言。
- ✅ Resolved review finding [high] CR4-008：metadata 异步读取和同步 COMMIT fence 使用固定小块 `MAX+1` sentinel，增长超限首字节即停。
- ✅ Resolved review finding [high] CR4-009：Worker 成功输出增加每文件/请求 facts 与估算 structured-clone 字节预算，稳定返回 `ANALYZER_RESOURCE_LIMIT`。
- ✅ Resolved review finding [high] CR4-010：SQLite CHECK normalizer 精确保留单引号 literal 的大小写、空白与内容，篡改 literal 的 v3 schema 会被拒绝。
- ✅ Resolved review finding [medium] CR4-011：封闭 typed Worker error 精确区分 CONFIG_INVALID、RESOURCE_LIMIT、PROTOCOL_INVALID 与 EXECUTION_FAILED。
- ✅ Resolved review finding [medium] CR4-012：metadata 深度只在新增层时消耗，允许第 128 层后的稳定确认，环和第 129 层仍 fail-closed。
- ✅ Resolved review finding [medium] CR4-013：default/named/mixed/import-equals 的 type-only `require` 不再注册值遮蔽。
- ✅ Resolved review finding [medium] CR4-014：retained memory 保守覆盖 Program、SourceFile/AST 与目录索引，并限制对象数量。
- ✅ Resolved review finding [low] CR4-015：仓库布局文档改为数量、字节和深度预算内迭代至稳定。
- 完成 DS fix QA production defect attempt 7：分离 SQLite 全图摘要与公共 ServiceStatusV1 hierarchy 摘要，修复模块提交后 Analyzer 配置失败触发的 canonical 状态拒绝；应用层与真实进程回归证明失败分类、stale、revision、连接连续性和 SQLite composite atomicity 均满足合同，Story/Sprint 保持 `review`。
- QA attempt 1 补充 consulted-config COMMIT fence 与真实 graph-service 进程两条高风险集成/E2E 回归；前者通过，后者暴露 Analyzer 配置失败后 `service/status` 生成非法 canonical 状态的生产缺陷，返回 `needs-fix` 交由新的 DS 处理。
- 完成 DS validation-fix attempt 6：仅对缓存命中统计做显式类型收窄，`undefined` 会直接使测试失败，原有“后续分析命中缓存更多”运行时断言保持不变；未运行完整 `architecture-required`，留给新的 VALIDATION。
- 完成 DS fix review findings attempt 5：CR attempt 3 的 16 项 finding 全部按 RED→GREEN→REFACTOR 处理，新增测试均已转绿，并保持 Analyzer/GraphPatch/GraphStore 单一原子主干与 Story 1.6/1.7/1.9 职责边界。
- 持久 Worker 现复用项目级 TypeScript `Program`、SourceFile、目录索引和模块解析缓存，并同时限制项目数、条目数、UTF-8 字节与语法事实；include/exclude/depth、referenced project、循环词法作用域和 relationType/qualifier 协议均封闭验证。
- 配置与 metadata 链现对缺失 extends 候选 fail-closed，把 consulted/absent 路径同步纳入 read set、原生 watcher semantic sequence 与 COMMIT fence；固定轮询改为节点/文件/字节/深度预算闭包，候选在 Worker 和主进程物化前流式早停。
- 模块目标解析不再把 root 内非 manifest bare alias 伪造为 npm，package version 缺失与严格 SemVer 状态分离；Analyzer 配置、资源预算和 metadata 不稳定使用可观察的扫描失败分类并在已有 revision 时标记 stale。
- SQLite 增加精确 Evidence 关联索引、同锁迁移路由和 workspace/edge/source 拓扑验证；DDL allowlist、迁移恢复、并发 opener、跨 workspace 污染、EXPLAIN 与 10,000 行规模测试均通过，规范 UTC 毫秒时间在 domain/application/SQLite 三层统一校验。
- 完成 DS validation-fix attempt 4：仅修正 NodeNext `impliedNodeFormat` 测试夹具的公共类型上下文，保持 `ts.ResolutionMode` 生产合同、`ESNext` 断言和 File List 57/57 不变；未运行完整 `architecture-required`，留给全新 VALIDATION。
- 完成 CR attempt 2 的 16 项 patch finding；全部新增回归均先失败后通过，并保持 Story 1.6/1.7/1.9 的 symbol、Evidence 生命周期和 workspace 聚合边界不变。
- TypeScript 配置解析现完整执行 include/exclude/depth，并对根外 reference/path options fail-closed；源码未归属封口项目时，已解析内部目标降为 medium。
- NodeNext SourceFile 使用最近 package.json 推导的 `impliedNodeFormat`；package metadata 只读取最后一个 node_modules 包根的精确 manifest，缺失或非法 metadata 只诊断不建伪 high 边。
- Analyzer 改为单个持久 Worker + requestId 协议，复用有界字节/语法事实缓存；scanner 源码落入 SharedArrayBuffer，相同 path/contentHash 的后续 broker 请求不再重复传正文。
- module manifest 与 SQLite ownership 分别建立请求级 WeakMap 索引和单遍分组；200×200 回归把 manifest path 读取从 20100 次压到线性上界，ownership 以每行常数属性访问完成。
- Worker `ok:true` payload 现深度校验所有诊断、binding、relation、qualifier、target、range 与词汇；namespace export、被遮蔽 require、空/孤立代理项 ModuleExportName 和严格 SemVer 均按封闭合同处理。
- watcher Worker 补齐 metadata helper 注入，原生 README change 只推进 raw sequence；SQLite v3 逐个解析并精确比较 CHECK，运行时封闭 relation/provenance/evidence kind。
- `tests/unit/index-read-set.test.ts` 仅因 CR2 finding 6 的 review-record/task AC scope 修改：通过原生 watcher 回归证明 README 非语义变更只推进 raw sequence，不推进 semantic/commit fence sequence；因此该新增回归路径归属 Story 1.5。
- `packages/adapters/store-sqlite/src/migrations/001-bootstrap.ts` 仅因 CR2 finding 13 的 review-record/task AC scope 修改：改用 IMMEDIATE 锁并在锁内重读 absent/v1，保证并发幂等。
- 完成 CR attempt 1 的 18 个 action items；新增回归测试均先失败后通过，并保持 Story 1.6/1.7/1.9 边界不变。
- 配置链现在由 graph-service 权威入口驱动，TypeScript 公共 API 递归解析 extends/references，并冻结 source→project 每文件选项、保序 `rootDirs` 与受控 `paths`。
- Worker/解析链现已传递 AST usage resolution mode、补齐 declaration-level 模块请求与 local export seed，并封闭 npm specifier、resolved-invalid metadata、AD-21 language/confidence 合同。
- metadata broker、Analyzer/runtime shutdown 与 SQLite v3 migration/reopen 均增加竞态、拓扑、精确 DDL 和 UTC 不变量验证。
- 使用 TypeScript 6.0.3 稳定公开 Compiler API 与真实 Worker 完成受控 TS/TSX/JS/JSX 模块分析，支持有界启动、取消、异常收敛和幂等关闭。
- 完成 Analyzer 配置两阶段封口、consulted file broker、JCS/SHA-256 digest 与提交前同步复核；`workspacePackages` 保持 Story 1.9 前的空集合。
- 完成 AD-24 语法映射、Node built-in/内部文件/外部 npm purl/unresolved 目标优先级、稳定诊断、source FactBatch、AD-21 Evidence ID 与本地 export seed。
- hierarchy 与 source facts 通过 `CompositeGraphPatchV1` 在单次 logical rebuild、单事务和至多一个 revision 中提交；import/源码删除会退休旧 Evidence、无支撑边和节点。
- 新增 SQLite v3 八表兼容迁移与严格恢复回验，保持 v1/v2/v3 重启兼容、未来版本 fail-closed、WAL reader 与 fault rollback 不变量。
- 新增 `typescript-module-analysis-v1` blocking gate；公共 RPC/Schema 表面无变化，因此未修改 `ci/public-capability-gates.v1.json`。
- 将架构 runner 单 gate 预算对齐真实 contract 上界，并将 Windows 进程树默认清理宽限对齐已验证的 10 秒，消除门禁超时污染。
- 未触碰 `.tmp-story-1-4-*`、`.playwright-cli/` 与 `想法.md`。

### File List

- `_bmad-output/implementation-artifacts/1-5-提取模块依赖并解析目标.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `apps/graph-service/src/analyzer-config.ts`（CR9-001/008）
- `apps/graph-service/src/index-job-runtime.ts`
- `apps/graph-service/src/index-read-set.ts`（CR9-001）
- `apps/graph-service/src/index.ts`
- `apps/graph-service/src/workspace-scanner.ts`
- `ci/quality-gates.v1.yaml`
- `docs/ci/story-1-5-typescript-module-analysis.md`
- `docs/planning/story-1-5-authority.md`
- `docs/protocol/service-control-v1.md`
- `docs/repository-layout.md`
- `packages/adapters/analyzer-typescript/package.json`
- `packages/adapters/analyzer-typescript/src/analyzer-worker.ts`
- `packages/adapters/analyzer-typescript/src/index.ts`
- `packages/adapters/analyzer-typescript/src/module-syntax.ts`
- `packages/adapters/analyzer-typescript/src/module-target-resolver.ts`（CR9-003/006）
- `packages/adapters/analyzer-typescript/src/typescript-analyzer.ts`（CR9-003/005）
- `packages/adapters/analyzer-typescript/src/worker-analysis.ts`（CR9-002/005/006/007/008）
- `packages/adapters/analyzer-typescript/tsconfig.json`
- `packages/adapters/store-sqlite/src/index.ts`
- `packages/adapters/store-sqlite/src/migrations/001-bootstrap.ts`（CR2 finding 13 review-record 例外）
- `packages/adapters/store-sqlite/src/migrations/003-module-dependencies.ts`
- `packages/adapters/store-sqlite/src/sqlite-graph-store.ts`
- `packages/application/src/index.ts`
- `packages/application/src/indexing/analyzer-config-snapshot.ts`
- `packages/application/src/indexing/composite-graph-patch-builder.ts`
- `packages/application/src/indexing/module-fact-batch.ts`
- `packages/application/src/ports/analyzer-port.ts`
- `packages/application/src/ports/graph-store-port.ts`
- `packages/domain/src/graph-identity.ts`
- `packages/domain/src/graph-patch.ts`
- `packages/domain/src/hierarchy.ts`
- `packages/domain/src/index.ts`
- `packages/domain/src/module-dependency.ts`
- `packages/service-client/src/connection.ts`（VALIDATION11 validation-record 例外）
- `pnpm-lock.yaml`
- `scripts/architecture/check-dependency-boundaries.mjs`
- `scripts/ci/run-architecture-required.mjs`
- `scripts/ci/run-process-with-deadline.mjs`
- `scripts/ci/verify-typescript-module-analysis-v1.mjs`
- `tests/contract/dependency-boundary-negative.test.ts`
- `tests/contract/failure-propagation.test.ts`
- `tests/contract/graph-service-process.test.ts`
- `tests/contract/quality-gates-manifest.test.ts`
- `tests/contract/root-toolchain.test.ts`
- `tests/contract/service-client-control.test.ts`（VALIDATION11 validation-record 例外）
- `tests/unit/analyzer-config-capture.test.ts`（CR9-001/002/005/006/007/008）
- `tests/unit/analyzer-config-snapshot.test.ts`
- `tests/unit/composite-graph-patch.test.ts`
- `tests/unit/index-job-runtime.test.ts`
- `tests/unit/index-read-set.test.ts`（CR9-001）
- `tests/unit/module-dependency-domain.test.ts`
- `tests/unit/module-fact-batch.test.ts`
- `tests/unit/process-deadline.test.ts`（CR6-005/011 review-record 例外）
- `tests/unit/sqlite-graph-store.test.ts`
- `tests/unit/sqlite-module-dependencies.test.ts`
- `tests/unit/typescript-analyzer-worker.test.ts`（CR9-003/005）
- `tests/unit/typescript-module-resolution.test.ts`（CR9-003）
- `tests/unit/typescript-module-syntax.test.ts`

## Change Log

- 2026-07-31：完成 DS systemic correction attempt 17；以根 metadata absent/read-set、嵌套配置 base path、project 完整性置信度、双侧 Worker admission、统一 npm specifier 分类、完整前缀后代枚举和 ENOTDIR 缺失语义关闭 7 项 CR9 finding；聚焦 116/116、官方 verifier 280/280 + 6/6、相关 type/lint/marker 通过，Story/Sprint=`review`，下一步为独立 VALIDATION。
- 2026-07-29：完成 DS validation-fix attempt 16；以摘要校验的预编译 Windows Job host 与 `-EncodedCommand` 消除完整并行 unit 下短 Git helper 的动态编译/stdin 启动竞争，保留 detached-grandchild 与 fail-closed 语义；完整 unit/contract、官方 Story verifier、type/lint 通过，Story/Sprint=`review`，完整 `architecture-required` 留给 VALIDATION17。
- 2026-07-29：完成 DS fix CR8 findings attempt 15；修复 6 项 review finding，统一 TypeScript host path identity 与混合大小写 package boundary，补齐 service-client clean build、Windows Job Object 树收敛、workspace package root/name snapshot 和安全 npm subpath；通过指定聚焦/合同/type 与官方 Story verifier，保持 60 路径 ownership，Story/Sprint=`review`，完整 `architecture-required` 留给下一独立 VALIDATION。
- 2026-07-28：完成 DS validation-fix attempt 14；修复 Windows 50ms absolute deadline 在 `taskkill=128` 后过早取后代快照导致的 `EPROCESSCLEANUP` 误分类，在原 10s cleanup grace 内等待根 `close` 后执行单次后代证明；通过指定稳定性、完整 unit/contract、专项 verifier 与静态门禁，保持 60/60/60，Story/Sprint=`review`，完整 `architecture-required` 留给 VALIDATION15。
- 2026-07-28：完成 DS fix CR7 findings attempt 13；修复 7 项 review finding，补齐无 ASCII 卷语义、完整 case-insensitive 虚拟 host、pnpm workspace metadata fence、500000 LOC 有界 Program 复用、taskkill 128 后代证明、clean-checkout verifier 拓扑与 exports 拒绝诊断；通过指定稳定性、完整 unit/contract 与静态门禁，保持 60/60/60，Story/Sprint=`review`，完整 24/24 留给 VALIDATION14。
- 2026-07-28：完成 DS fix CR6 findings attempt 12；修复 13 项 review finding，增加真实文件系统大小写语义、带点目录 exclude、主线程 metadata case-fold、分区有界 Program 复用、Windows 树级清理、MAX+1 早停、Worker 协议范围/confidence、npm/template/timer/BOM 回归；通过指定稳定性、完整 unit/contract 与静态门禁，File List/controller+授权/实际有效变更为 60/60/60，Story/Sprint=`review`，完整 24/24 留给 VALIDATION13。
- 2026-07-28：完成 CR code review attempt 6；完整审查 59 个 Story-owned 路径，保留 13 项可复现 finding 为 action items，未实施修复，Story/Sprint 转为 `in-progress`。
- 2026-07-28：完成 DS validation-fix attempt 11；以 test-only discovery 注入稳定 stale-token 三次连续分类，以 Windows 独立进程组即时终止和 `taskkill /T /F` 有界收敛修复后代泄漏；通过完整 contract/unit、指定稳定性与静态门禁，File List/ownership/实际有效变更更新为 59/59/59，Story/Sprint=`review`。
- 2026-07-28：完成 DS validation-fix attempt 10；以可注入 I/O 边界消除 Analyzer 配置捕获在组合负载下的测试级 timeout，精确恢复 VALIDATION10 污染的根 `.gitignore`，通过两个曾失败 gate 与指定回归，并保持 Story File List/controller ownership/实际有效变更 57/57/57、Story/Sprint=`review`。
- 2026-07-28：完成 DS fix CR5 findings attempt 9，修复 CR attempt 5 的 18 项 finding；补齐 Analyzer fence/预算、Worker 协议与增量资源 sink、SQL tokenizer/index_xinfo、Evidence/payload 恢复、Story 1.7 回收边界、Windows watcher 与不可缩小 verifier，并通过指定验证，Story/Sprint 保持 `review`。
- 2026-07-28：完成 DS fix review findings attempt 8，修复 CR attempt 4 的 15 项 finding；增加 blocked/absent resolution fence、完整 composite snapshot、host target 语义校验、watcher 竞态、请求索引、MAX+1 读取、Worker facts/内存预算、quote-aware CHECK 与 type-only require 回归，并通过指定验证，Story/Sprint 保持 `review`。
- 2026-07-28：完成 DS fix QA production defect attempt 7；修复 `QA1-GRAPH-SERVICE-INVALID-FAILURE-STATUS`，补充 canonical 状态根因回归并通过指定 build、真实进程、runtime、Story verifier、type 与目标质量门禁，Story/Sprint 保持 `review`。
- 2026-07-28：完成 QA generate high-risk integration/E2E coverage attempt 1；新增两条 blocking 回归，consulted-config COMMIT fence 通过，真实进程错误分类回归暴露 `SERVICE_PROTOCOL_INCOMPATIBLE` 生产缺陷，Story/Sprint 保持 `review` 并进入 `needs-fix`。
- 2026-07-28：完成 DS validation-fix attempt 6，修复 VALIDATION attempt 6 的缓存命中统计 TS2345，并通过 type、聚焦测试与官方 Story verifier，Story/Sprint 保持 `review`。
- 2026-07-28：完成 DS fix review findings attempt 5，修复 CR attempt 3 的 16 项 finding，增加量化性能/内存/SQLite 回归并恢复 Story/Sprint 为 `review`。
- 2026-07-27：完成 DS validation-fix attempt 4，修复 VALIDATION attempt 3 的 `impliedNodeFormat` 类型门禁失败，`pnpm type` 与聚焦测试通过并恢复 Story/Sprint 为 `review`。
- 2026-07-27：完成 TypeScript/JavaScript 模块依赖分析、配置封口、复合原子提交、SQLite v3、真实 blocking gate 与全量回归，状态更新为 `review`。
- 2026-07-27：完成 DS fix review findings attempt 1，修复 18 项 CR finding，补充必要回归测试并恢复 Story/Sprint 为 `review`。
- 2026-07-27：完成 DS fix review findings attempt 2，修复 16 项 CR finding，增加可度量性能回归并恢复 Story/Sprint 为 `review`。
