---
topic: BMad Project Code Graph UX
updated: 2026-07-16T14:11
---

- (decision by user) 将产品简报、PRD、PRD 补充说明及两份研究报告全部作为 UX 设计依据。
- (event) 创建 UX 工作区并进入 Discovery。
- (decision) MVP 形态为 VS Code 桌面插件与本地 CLI，聚焦 TypeScript/JavaScript 项目；不设计独立 Web Dashboard、账号、云协作或 hosted PR 界面。
- (decision) 体验核心是帮助开发者在当前代码上下文中判断影响范围与结构违规，图谱是解释手段而非产品目标。
- (decision) 已确认的核心体验表面：首次索引与状态恢复、项目结构概览、当前文件邻域、Findings/循环/规则风险；PR 摘要与 AI 上下文以本地输出为主。
- (decision) 图形视图必须有列表或表格等价表达；缓存、刷新中、最新、过期和失败状态必须清晰可辨。
- (assumption) 首批用户类型、VS Code 内 TreeView/Webview/Panel 的职责边界、视觉身份、严重级别编码和 Finding 处置方式仍待用户确认。
- (note) 关注项：键盘与屏幕阅读器、高对比主题、减少动画、窄侧栏与编辑器布局、图谱性能、告警噪声、数据置信度、本地隐私、CLI 与插件术语一致性。
- (event) 五份已确认资料完成 UX 证据提取与交叉核对。
- (decision by user) 采用快速路径：批量补齐缺口，以 [ASSUMPTION] 标记未确认决定，跳过 Discovery 创意工具。
- (assumption) 视觉层继承 VS Code 原生主题、字体、焦点与组件语义，仅定义图谱和风险表达所需的增量规范。
- (assumption) 首版采用 Activity Bar/TreeView 作为窄入口、编辑器 Webview 作为主图表面、Problems/列表承载 Findings，Command Palette 承载 rebuild 与导出。
- (event) 快速路径 DESIGN.md 与 EXPERIENCE.md 草案已生成并通过 YAML、token、组件同名、来源与 UJ-1 至 UJ-5 机械覆盖检查。
- (note) 快速路径未生成 mockup/wireframe；当前所有 IA surface 均为 spine-only。
- (decision by user) 直接定稿：跳过 Reviewer Gate，不生成关键界面 mock；全部 IA surface 按 spine-only 交付。
- (assumption) 文档内保留的 [ASSUMPTION] 均作为非阻塞设计假设进入定稿，后续可通过 bmad-ux Update 调整。
- (event) 结构编辑标准审校完成：参考契约结构清晰，无需重组或删减。
- (event) 文字编辑标准审校完成：修正少量中英文混写与一处缺失空格。
- (event) spines finalized
- (event) 检测到 PRD 与 Addendum 于 2026-07-13 更新为 final，启动 UX Update。
- (override by user) 按影响分析建议更新界面规范：以 final PRD 覆盖旧 UX 中关于首批用户、UJ-5、规则 v1、图预算和 PR 摘要阶段的假设。
- (decision) 保持现有 VS Code 导航框架、组件集合、视觉 token 与整体布局；不新增主 surface，不重新设计视觉系统。
- (change) GraphNode 与 Overview 补充 workspace package、跨 package 依赖和 monorepo 识别降级；FindingRow 补充稳定规则 ID、warning/error、配置位置和修复提示。
- (change) StatusBanner 与 IndexSummary 补充超规模索引的进度、取消和重建；State Patterns 补充规则配置错误、monorepo 降级、已取消索引和遥测状态。
- (change) Settings & Rules 补充遥测默认关闭、显式 opt-in、可查看当前状态和随时关闭；PR Summary 标记为 Beta+，不阻塞首个可用版本。
- (decision by user) 重新定稿 UX Update；沿用跳过多代理评审与界面 mock 的决定，全部 IA surface 继续按 spine-only 交付。
- (assumption) Open UX Assumptions 中的容器布局、Finding 处置、i18n、候选快捷键、响应断点与 ContextLock 会话范围均为非阻塞假设，允许进入定稿并在实现验证后更新。
- (event) PRD 对齐更新完成结构审校：参考契约结构清晰，无需重组或删减。
- (event) PRD 对齐更新完成文字审校：统一定稿表述与超出标准验收规模措辞。
- (event) spines finalized
- (decision) 采用已批准的 sprint-change-proposal-2026-07-15 作为本次 UX Update 输入；Activity Bar/TreeView、编辑器 Webview、Problems 与 Command Palette 的 MVP surface 职责正式采用，首个真实 Webview 切片只验证视觉适配。
- (decision) VS Code 人类可读界面支持 zh-CN 与 en，跟随 VS Code locale，未知 locale 回退 en；JSON、稳定错误 code、状态枚举和 Schema 不本地化。
- (decision) ContextLock 只存在于当前 extension-host 会话：Webview reload 可从扩展内存恢复，窗口 reload、VS Code 重启或重新打开工作区后清除；workspaceState/globalState 不保存固定标记。
- (change) State Patterns 与 UJ-1 增加 Workspace untrusted：不启动服务、不读取项目、不执行 Git 分析，唯一主动作是管理 Workspace Trust。
- (change) GraphNode、NodeDetails、图例、列表与可访问性契约补充 Node built-in：external-package 的 node-builtin 子类型，使用 node: 前缀、Node 内置模块标签、独立模块图标和点线轮廓。
- (decision) 沿用用户既有定稿决定：跳过多代理 Reviewer Gate 与独立 mockup，全部 IA surface 继续按 spine-only 交付；真实 Webview 证据进入首个实现切片。
- (event) 本次 UX 契约更新完成结构审校：DESIGN canonical order 与 EXPERIENCE 必备结构保持完整；修正旧 reconcile-prd.md 的过期未确认结论，并新增 sprint change proposal 对账记录。
- (event) 本次 UX 契约更新完成文字审校：Node built-in 技术类型、Node 内置模块人类标签、locale 与 Workspace Trust 术语清晰一致，无需进一步改写。
- (event) spines finalized
- (decision) 采用已批准的 sprint-change-proposal-2026-07-16 作为本次 UX Update 输入；保持现有 IA 主路径、surface 职责、组件集合与视觉系统，只收紧导出、可访问性、快捷键和真实 Webview 验收合同。
- (change) ExportPreview 仅允许 artifactStatus=complete 且具有 artifactId、revision、policy 与 contentDigest 的不可变 artifact 执行复制或写出；生成失败与目标操作失败拆分为不同状态，禁止复制部分内容。
- (override) WCAG 2.2 AA 与图节点至少 24×24 CSS px 可选区域由非阻塞假设升级为 NFR-18/NFR-19 强制要求；仅验证证据形式仍需实现阶段闭合。
- (change) 候选快捷键按 Windows/Linux 与 macOS 分平台定义，并在 VS Code 1.125.0、最新稳定版和前一稳定版检查宿主默认、Accessibility Help、产品命令和系统保留组合；冲突平台不注册默认绑定，Command Palette 始终等价可用。
- (change) 900/600/360 CSS px 保留为可校准候选阈值；真实 Webview 必须覆盖指定宽度、主题、100%/200% 字号、键盘与 NVDA/VoiceOver/Orca spot check，并记录宿主版本、焦点、溢出和已知限制。
- (decision) 沿用既有定稿决定：跳过 Reviewer Gate 与独立 mockup，全部 IA surface 继续按 spine-only 交付。
- (change) 权威聚合术语同步到 UX：module 不是持久实体，用户可见模块必须映射为 directory 投影叶或 recognized workspace package；界面与导出同时携带 groupBy 和规范 ID。
- (change) Project Overview 只有 freshness=current 且 completeness=complete 时显示正式排名；stale/partial 只显示带限制说明的非正式结果，稳定排序后再按预算截断，展开聚合创建新的局部查询。
- (change) UJ-4 明确 PR 摘要无需结构性重分析、最多措辞或格式调整；UJ-5 明确允许范围与禁止边界、structure-only artifact 约束 AI、修改后运行 check/impact 的闭环。
- (change) 最终复核收紧 Export ready：动作启用必须同时具备 artifactId、revision、requestedPolicy、effectivePolicy、containsSource 与 contentDigest；Export generating 仅显示进度和取消/返回。
- (change) GraphNode 将 file、directory projection leaf、workspace package、external-package 定义为视觉节点类别，不把查询投影叶误写为持久顶层实体。
- (event) 2026-07-16 UX Update 完成结构审校：两份 spine 继续采用 reference-contract 结构，DESIGN canonical order 与 EXPERIENCE 必备结构完整，无需重组或删减。
- (event) 2026-07-16 UX Update 完成文字审校：修正上一份有效 artifact 与本次失败状态视觉分离的一处句法歧义，其余术语保持一致。
- (event) 2026-07-16 UX Update 最终覆盖检查通过：11 个组件同名、7 个来源存在、完整 artifact 六字段、导出状态拆分、可访问性强制要求、快捷键与真实 Webview 矩阵全部闭合。
- (event) spines finalized
